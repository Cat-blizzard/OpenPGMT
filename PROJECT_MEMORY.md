# PGMT 项目状态

此文件只记录 `/data/jxc/ld/PGMT`，不替代父目录其他项目的研究记忆。

## 2026-09-22：三种子站立学习与参考 v4 完成（最新）

- 已完成用户授权的两条件×3 种子×16 环境×24 步×100 更新，恰好 230400 物理 transitions、600 PPO 更新；保存 921600 环境子步速度。六个 GPU 子进程全部退出，最后 GPU 9 复查为 28 MiB、0% 利用率，未启动任何物理评估或额外回放。
- 种子 0/1/2 原目标已结束 episode 平均时长 1.261/1.178/1.314 s，候选 1.436/1.354/1.419 s；各条件三种子均值分别 1.251±0.069 / 1.403±0.043 s。六场均无 10 s timeout，候选各种子后 20 更新时长均低于前 20 更新；这仍是训练统计，不能替代冻结策略评估，也不能据 100 更新断言不可学习。
- 三个种子配对初始化均验证：除 Actor 目标偏置外参数完全相同，初始观测、std、环境/PPO 配置一致。种子 0 两条件前 20 更新与旧实验动作、奖励、更新前后参数和子步速度完全一致。
- 600 次 CPU 审计动作/log-prob/价值/GAE/奖励与 EE 时序/KL 通过，接受 Actor 优化器步共 1881。平均 KL 均≤0.019994，但最大单状态 KL 约 0.528；不能误称为逐状态上限。第 100 更新的下肢/辅助头 Actor 梯度余弦 −0.874 至 −0.974，是冲突证据而非原因证明。
- CPU 审计两次精度边界保留原失败记录：u73 float32 CPU critic 与 GPU 差 0.000488，float64 前向将最大误差降到 0.000100；u98 近零 GAE 的 float64 理想递推差 0.000209，而独立 NumPy FP32 与保存结果完全一致。最终采用双精度价值、执行精度 GAE 加双精度舍入误差界，原容差和训练代码不变。最终输出 `cpu_audit_v3`。
- 10 个环境子步超过诊断阈值 45 rad/s，最高 54.800。三个 seed2 踝峰值出现在较小倾角 12°–22°，不能全部归为末期跌倒；control 第 1855 步首子步 46.696，前估计 PD 仅 −0.681 Nm、目标变化 −0.0141 rad，随后速度回落，0.42 s 后 episode 结束。CPU 上下文见 `speed_event_context.json`；缺少这些事件的子步位置/接触，物理准确性仍待解释，本批没有追加 GPU 回放。
- 新冻结清单及六场启动器保留旧配置；独立 10 秒评估入口已准备，12 个 checkpoint 指纹已封存，24 个实际任务通过 CPU 选择检查，六组四起点/500 步配置通过 mock 检查。评估最多 48000 transitions，物理执行后置；代码指纹、配置与共同存活窗口检查已接入，10 秒物理 timeout 分支尚未验收。
- 原八片段 v4 独立生成：联合根/腿拟合、连续全身非穿地高度修正、50 Hz 原生网格与独立接触查询。支撑脚 >2 cm 的比例从同网格 v3 的 9.35%–45.19% 降到 0%–5.11%。50 Hz 标签差异 0，最小间隙约 0.50 mm；100 Hz 无穿地、最小间隙 0.286 mm，但标签差异 0.1%–0.4%。
- 同一躯干相对解剖目标误差增加 0.19–3.67 mm，部分峰速上升，两个片段达到 200 次迭代上限，源腰贴限保留。v4 仍 training_approved=false，不加入站立或正式训练。初次信赖域初始化问题已修正，trial1 和源码快照保留。
- 279 个原有源码/数据/静止 fixture 文件指纹不变；六场冻结源码一致。最终 874 passed、2 skipped。完整报告 `docs/standing_multiseed_20260922.md`；原始目录 `runs/standing_multiseed_20260922/`；校验 `docs/results/standing_multiseed_verification_20260922.json`。
- 后续应使用冻结 initial/final checkpoint 完成已准备的匹配评估，再决定下一项有界学习实验；保留较小倾角速度瞬态与参考 v4 的剩余问题，正式 Stage 1 扩规模仍未启动。旧 v4 策略的 30 秒评估继续后置。

## 2026-09-22：后续计划细化（仅规划，尚未执行）

- 核查当前启动器/冻结清单仍锁定 seed=0、20 更新；旧三种子脚本使用不同初始化且自动串接评估，旧评估入口只支持 5 秒、未加载冻结站立 fixture，不能直接用于本批。
- 建议先在 CPU 准备独立批次清单、逐种子冻结配置、六场启动/预算/退出清理与独立 10 秒评估入口，保留原清单和 checkpoint。训练种子 0/1/2，每种子 control/candidate 配对初始化。
- 下一轮建议预算仍为两条件×三种子×16 环境×24 步×100 更新，共 230400 transitions；保持 PD 80/2、奖励、网络、PPO、物理、终止及 1000 更新学习率日程。到预算保存并释放 GPU。
- initial/final × 两种动作模式 × 四起点的评估单独后置，最多 48000 transitions；计数包含向量环境实际推进的全部步，不只统计活跃 episode。100 更新不保证学会，需配对行为证据才能判断收益。
- CPU 参考下一步为源支撑核查、根与腿关节联合支撑及 50 Hz 插值一致性；有依据后另存新版本，v3 继续隔离。此轮只更新计划，没有运行新训练、评估或生成参考数据。

## 2026-09-22：同动作回放与支撑参考 v3 完成（最新）

- 执行用户授权的 CPU 对齐、必要时最多 9600 transitions 回放及独立 v3；两条件各 16×300 步，共 9600，0 PPO。GPU 子进程 3802203/3804292 已退出，后续全部 CPU。
- 回放 600 控制步的关节位置、根位置、全部子步速度和 reset 时机与原训练完全一致；原始/缓存/目标及子步接触更新时间检查通过。完整 38400 环境子步保留。
- 45.173/54.800 rad/s 左踝峰值均与跌倒触地同步，峰值左脚接触力约 567.3/404.1 N，开始时 PD 估计仅 −0.488/−3.390 Nm，下一子步才 50 Nm。
- 力矩是隐式 PD 估计、不是求解器实测。位置差分速度与末态速度不同（候选甚至反向），不能据此认证真实冲量准确性；未发现动作/读回错误，无需为单次跌倒峰值修改奖励或无限调参。
- 原 `pgmt/` 与旧 `data/` 源码未改；PD 80/2、奖励/网络/物理设置/终止规则保持。新增离线 `data/support_retarget.py` 是显式 opt-in，不修改默认导出器。
- BVH 层级确认旧上肢位置/旋转对应错位：LeftArm/LeftForeArm/LeftHand 是肩/肘/腕；旧位置 IK 将前两者对到肘/wrist_roll。v3 按对应骨长和源方向建目标，并固定腰为有界源姿态投影，防止 IK 以腰弯补身体比例。
- v3 用完整源序列低位静止脚趾估计一个源平面，保留连续支撑段，联合求解连续根高度（支撑+源轨迹先验+时间平滑+全身非穿地约束）。源支撑代理不直接充当导出接触标签；标签仍为脚几何/平地网格查询。
- 原八片段生成于 `lafan1_g1_asset_grounded8_v3`，原 77/v1 八/v2 八文件 SHA-256 均不变。三段慢走 IK 引入的腰贴限 41.3%/100%/89.3%→0%，slow_walk_0 保留源映射贴限 70.7%。
- v3 八段恢复部分接触，但源支撑脚 9.5%–45.3% 样本仍高于 2 cm；固定关节轨迹下，8.7%–86.7% 支撑帧单靠根平移不可能满足全部支撑约束。下一步需根与腿关节联合支撑，不能只调高度。
- 部分峰速上升：slow_walk_3 5.082→5.717 rad/s。两种 reset 的源帧接触/高度一致，lift=0；50 Hz 插值最深穿地 0.583 mm，接触标签差异逐段 0.2%–1.4%。v3 标记 training_approved=false，未加入站立或正式训练。
- 858 passed、2 skipped；定向 11 passed。报告 `docs/standing_replay_reference_20260922.md`；证据 `runs/standing_replay_reference_20260922/`。
- 后续可独立推进静止参考上的两条件三种子各 100 更新及后置匹配评估（既有预算），保留瞬态监控；本批未启动。参考 v3 的联合支撑/插值问题在 CPU 独立处理，旧 v4 评估继续后置。

## 2026-09-22：两条件站立学习 20 更新与参考病因定位完成（上一批）

- 训练入口加载冻结载荷候选和四起点，起点各重复四份；两组初始网络仅 Actor 最后偏置不同，探索标准差/观测/配置一致。
- 两条件各 16×24×20 更新，合计 15360 物理 transitions。已保存 checkpoint，GPU 子进程 3759602/3765095 均退出；后续 CPU 分析，不自动续训/评估。
- PD 80/2、奖励权重、网络/PPO 算法、A23 和终止规则未改。静止参考不含八个悬空运动片段。
- 全部 40 次更新 CPU 复核动作、log-prob、GAE、奖励/EE 时序和 KL 通过；接受 Actor 优化器步数 101/95，平均 KL 0.01924/0.01898。
- 训练内已结束 episode 平均时长 1.268/1.532 s，平均倾角 19.02/14.64°，关节 RMSE 0.11957/0.12331 rad。单种子且无 initial/final 冻结策略评估，不能归因为学习收益。
- 每环境总模拟预算只有 9.6 s，因此 10 s timeout 不可达、物理分支未覆盖；不报告该批 10 s 成功率。20 更新无明显改善不等于学不会。
- 完整 61440 环境子步保存；两组左踝 roll 各一次 >45 rad/s，峰值 45.173/54.800，出现在下落期间，非 reset 首步；控制步末已回落。接触/控制器具体因果未确定。
- 八段 v1 完整源序列锚定帧全部在评分片段外，low_motion_0/slow_walk_2 由右膝锚定，其余由右踝锚定。保持轨迹和单一平地时，没有兼顾全序列不穿地和片段 2 cm 接触的单一高度平移。
- slow_walk_0 源映射已 70% 越过腰 pitch 上限；其他三段解析未贴限而 IK 后 41.3%/100%/89.3% 贴限。向内 0.001 rad 扰动使原目标增加，指向源形态/位置目标问题；现有 smooth 项非时间平滑。
- low_motion_2 异常为 right_elbow 源帧 6375→6376：解析 +0.02029 rad，IK10 为 −0.29222，IK30 为 +0.01357，已定位迭代不足。
- 独立 `lafan1_g1_asset_grounded8_v2` 仅提高本次 IK 迭代 10→30，保持其余规则；整片段最大速度 8.7667→2.9547 rad/s。原 77 文件及 v1 八文件 SHA-256 均不变。
- v2 全部关节/接触查询和两种 reset CPU 一致性通过，1200 帧原目标值均未增加；足底仍悬空 6.48–27.24 cm，腰贴限保留，标记 training_approved=false，继续隔离。
- 852 passed、2 skipped。报告 `docs/standing_learning_20260922.md`；证据 `runs/standing_learning_20260922/`。
- 后续优先利用现有数据定位速度瞬态，以及源地面/支撑分段与 IK 形态目标。三种子 100 更新、额外评估和正式扩规模尚未执行，旧 v4 30 秒评估继续后置。
- 本批结束后的规划细化（尚未执行）：先 CPU 事件对齐；若缺证据，原布局 16 环境的已存动作回放各最多 300 步、两条件共最多 9600 transitions，0 PPO，停止即释放 GPU。先核对轨迹再解释事件；不把峰值必须低于 45 当作新的论文要求。参考修正独立在 CPU 推进，有依据后另存 v3，不阻塞已验证静止参考上的站立学习。详见 `docs/next_steps_20260922.md`。

## 2026-09-22：资产参考和载荷候选 A+B 完成（上一批）

- 用户授权只含参考/静态候选修正与四组 4 环境、最多 10 秒物理对照，总上限 8000 transitions。
- 执行 1596 transitions，0 次 PPO 更新；PD 80/2、奖励权重、A23、终止规则不变。四个 GPU 子进程已退出。
- 新数据 `data/processed/lafan1_g1_asset_grounded8_v1` 对应原冻结 8 片段，旧 `lafan1_g1_mesh_v2` 完整保留。
- 离线 FK/IK 显式绑定运行 URDF；USD 关节/质量/脚碰撞核查及实时 FK 读回通过。不能宣称所有 USD/URDF 全身碰撞面已逐面一致。
- 新 `flat_ground_v1` 参考地面映射世界 z=0，支持平地 Stage 1；两种 reset 的接触和高度复核一致。
- 新片段足底仍悬空 6.47–26.82 cm，慢走腰部贴限仍在，low_motion_2 最大关节速度 3.903→8.767 rad/s。新数据暂不用于正式训练。
- 一个冻结载荷候选：期望实际 q=0，目标 q*=q+tau/80；最大偏移 0.06177 rad，静态最大力矩占上限 9.88%。不添加外部力。
- 平均首次时长：原固定 1.47 s、原均值 1.46 s、候选固定 2.29 s、候选均值 2.27 s；均 0/4 完成 10 s。
- 共同窗口固定 PD：倾角 20.39°→3.28°，关节 RMSE 0.05109→0.01796 rad；无力矩饱和、无 >45 rad/s 子步。短时改善不是稳定站立或训练收益。
- EE 世界线加速度单位 m/s²，权重仍 −0.001。修复匀速参考首帧假加速度和 30/50 Hz 插值差分误差；最终契约 `reference_linear_derivatives_v3`，旧 checkpoint 不可静默续跑。
- 最后一项导数修正在物理运行之后；对该批静止参考 501 时刻的全部输出逐一确认完全一致，没有重跑 GPU。
- 847 项回归通过、2 项跳过。完整报告 `docs/asset_standing_validation_20260922.md`，原始目录 `runs/asset_standing_validation_20260922/`。
- 后续可按预登记分支考虑反馈站立学习，但 20/100 更新未启动。正式扩规模和 Stage 2 暂缓，旧 v4 的 30 秒评估继续后置。
- 不杀其他用户 GPU 任务；结束后的 GPU 9 再占用属于其他用户，非本任务残留。

## 上一批结束时的规划（本批首阶段现已执行）

- 原计划 `docs/standing_learning_plan_20260922.md` 的冻结目标接入、八片段定位和两条件各 20 更新均已完成，实际结果见本文件最新条目。
- 两条件三种子各 100 更新（230400 transitions）及匹配 initial/final 评估（最多 48000 transitions）仍只是后续预登记预算，尚未执行；先处理本批剩余证据问题。
