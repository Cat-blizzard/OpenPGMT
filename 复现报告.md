# PGMT 复现进展报告

> **2026-09-22 最新进度**：新版 v4 已完成三个种子的 64 环境 × 200 更新，但尚无一致的行为改善，最终策略评估后置。随后完成 8 场执行器对照及 6 场 reset 对照：保留资产逐关节上限；参考初始化降低首步冲击，但未改善整体稳定性，暂时保留默认 reset。当前没有本任务 GPU 进程，也未进入正式扩规模。详见 [v4 结果](docs/curriculum_critic_20260921.md)、[reset 结果](docs/reset_physics_20260922.md)、[图表与汇总](docs/results/README.md)及[下一步计划](docs/next_steps_20260922.md)。下文为历史记录。

> **2026-09-21 更新**：下文保留此前实验记录。旧 Stage 1 的 842-update run 已归档。新版物理接口检查与两阶段各 2 次 PPO 更新已完成，但训练稳定性未通过，正式训练尚未启动；详见 [本轮物理验收](docs/physics_validation_20260921.md) 和 [新版流程](docs/alignment_20260921.md)。

> **论文**: PGMT: Perceptive General Motion Tracking for Humanoid Robots（arXiv:2609.08511v2，2026-09-09，ZJU X-Mechanics + NUS MARMot Lab）
> **仓库**: `/data/jxc/ld/PGMT`（独立复现，官方代码未开源，完全依据论文正文与插图自行实现）
> **报告日期**: 2026-09-20 · 报告范围：首行代码（09-16）至今的全部工作

---

## 0. 一页速览

**一句话结论**：论文中**不依赖物理仿真器的全部组件**（数据管线、策略网络、奖励系统、地形系统、训练框架）已实现并通过 764 项回归测试；物理仿真侧已完成 Isaac Lab 环境验收——GPU9 上通过 USD 资产探针、箱体物理冒烟、CPU/GPU 数值合约检查，并完成**一次真实 G1 的最小 PPO 物理更新**（保存 17 MiB checkpoint）。**正式训练与论文指标复现尚未开始**，当前处于 Stage 1 扩规模训练的前夜。

| 维度 | 状态 |
|---|---|
| 代码规模 | 17 个提交（09-16 → 09-20，5 天），约 21,867 行 Python |
| 回归测试 | **764 passed**（实测 75 s，`/data/jxc/envs/pbfm_isaaclab` 环境） |
| 数据管线 | ✅ 77 个 LAFAN1 序列全部重定向 + IK 精修，产物验收零违例 |
| 策略/奖励/地形/训练框架 | ✅ 全部实现并有单测（Torch 协议闭环，Stage 1/2） |
| 物理仿真 | 🟡 Isaac Lab 单卡路径已验收（资产探针 + 冒烟 + 最小 PPO 更新）；**terrain 物理未接入** |
| 论文指标（Table II / Fig.3 / Fig.4） | ⬜ 未开始（M5/M6） |

---

## 1. 任务背景与复现目标

### 1.1 论文要解决什么问题

人形机器人（Unitree G1，29-DoF）在**非平整地形**上跟踪**任意参考动作**（走/跑/舞/跳/翻滚）。核心困难：平地 tracking 策略迁移到复杂地形会摔倒（论文中 PGMT-Pretrain 在地形上 completion 仅 40.02%）。PGMT 的贡献：

1. **Terrain-Glimpse Encoder**：不压缩整张高程图，而是由策略根据自身状态与未来动作**动态选择 4 个 5×5 局部 glimpse**（可微双线性采样，无需落足点标注）；
2. **两阶段训练**：Stage 1 平地 tracking 预训练（root-centric 误差 + recovery curriculum + adaptive sampling）→ Stage 2 感知注入（地形课程 + terrain-aware tracking relaxation + global position correction）；
3. **多头 critic**：upper/lower/aux 三头（Stage 1）→ 加 terrain 四头（Stage 2），论文 Fig.4 表明 split-return critic 在 Stage 2 才与 aggregate 拉开差距。

### 1.2 复现保真度目标（对标什么）

| 对标物 | 论文数值（4× RTX 5090 实测） | 说明 |
|---|---|---|
| Table II 主表 | PGMT-Ours：completion **87.81%**、L9 **83.33%**、body pos RMSE **0.0678 m**、body ori RMSE **18.14°**、joint RMSE **0.2299 rad**、contact F1 **0.8217** | 9600 matched episodes = 5 地形族 × 10 难度 × 192 集 |
| 消融 | NoHeight 86.53 / CNN 87.38 / 2Glimpse 85.17 / Fixed 86.38（completion %） | stairs L9 差距最大：Ours 78.12% vs 消融 64.58–69.79% |
| 基线 | PGMT-Pretrain 40.02 / RGMT-Reimpl 46.68 / SONIC v1.1 25.09 | |
| 训练成本 | 8.44 s/iter、29.23 GiB/卡、15k envs/卡 | 成本列按方案约定用我们自己的实测值回填 |
| Fig. 3 | 感知消融 L9 对比（Wilson 95% CI） | |
| Fig. 4 | split vs aggregate critic 训练曲线 | |

**范围**：纯仿真，不做真机/遥操作/LiDAR。论文未开源代码，所有未公开超参以**显式假设 A1–A22** 登记（`pgmt/cfg/assumptions.py`），最终报告逐项对照。

---

## 2. 复现路线与关键工程决策

| 决策 | 选择 | 依据 |
|---|---|---|
| 仿真器 | **Isaac Lab 2.3.2.post1**（Isaac Gym PP4 保留为备选，tar 已就位未启用） | 服务器已有授权 ProtoMotions G1 USD 资产 + py3.11/torch 2.7 cu128 环境；PP4 需要旧 ABI（py3.9 + torch 2.1.2 cu121），与驱动 580 组合风险高 |
| 代码架构 | 环境层与策略层**解耦**：观测/奖励/课程逻辑不依赖仿真器，`g1_env.py` 提供 torch 批量适配器 + Isaac Lab shell | 迁移仿真器成本可控；无 GPU 时也能回归 |
| 骨架参考 | human2humanoid（OmniH2O）：G1 资产、环境框架、重定向思路、奖励核函数 | PGMT 论文明示继承的 tracking 实现 |
| 数据 | LAFAN1（77 序列，Mixamo 骨骼 BVH，研究用途免费） | 论文指定 |
| 训练硬件 | 10× RTX 5880 Ada（sm_89，48 GB/卡），驱动 580 / CUDA 13.0；每卡独立一个 run | 48 GB > 论文 5090 的 32 GB，15k envs 显存余量更大 |
| 单卡策略 | Isaac Sim 路径用 `--device cuda:9` **而非** `CUDA_VISIBLE_DEVICES` 隔离 | 实测：环境变量隔离会让 GPU foundation 进入 CUDA bad state，箱体冒烟失败 |

---

## 3. 总体进度

### 3.1 里程碑状态

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 | 服务器环境 + 物理冒烟 + G1 资产验收 | ✅ Isaac Lab 路径完成（09-20 GPU9 验证批次） |
| M1 | LAFAN1 下载 + BVH 解析 + Mixamo→G1 重定向 | ✅ 完成 |
| M1.5 | 全链 IK 精修 + 接触标签统一协议 + ground 类裁定 | ✅ 完成（A17 已改判保留全集，v2 锚定后全集重生成） |
| M2.0 | 观测契约（96 维 / 历史缓冲 / 6D 旋转 / 特权观测表） | ✅ 完成 |
| M2.0b | 奖励规格（Table I 28 项 + Eq.10 松弛 + 语义对照表） | ✅ 完成 |
| M3 | 地形系统（5 族 × L0–L9 + 高程图 + 课程 + 兼容规则，纯逻辑） | ✅ 完成（mesh 注入归 M2） |
| M2 | Stage 1 平地 tracking 预训练 | 🟡 框架全通；真实物理**最小 PPO 更新**已通过，扩规模训练未开始 |
| M4 | Stage 2 感知注入 | 🟡 Torch 协议路径全通（四头 critic / elevation / terrain-contact）；真实 terrain 物理未接入 |
| M5 | 基准评估（9600 episodes）+ 消融 | ⬜ 未开始 |
| M6 | 基线（RGMT-Reimpl）+ 最终报告 | ⬜ 未开始 |

### 3.2 时间线（git 实录）

| 日期 | 提交 | 内容 | 测试数 |
|---|---|---|---|
| 09-16 04:50 | `20920af` | M1 数据管线 + M2 策略模块，第一份可运行代码 | 74 |
| 09-16 13:30 | `820a643` | M1.5 收尾：全链 IK 精修 + 接触标签协议 + ground 裁定 | — |
| 09-16 19:51 | `b2bf900` | M2.0 观测契约 + 奖励规格（Table I / Eq.10）+ 指标解耦 | 317 |
| 09-16 21:19 | `394b482` | M2 奖励层 + 终止条件 + M3 地形系统 | 623 |
| 09-16 23:01 | `59a0075` | 修复 tracking contracts / IK / 评估安全 | — |
| 09-18 | `15a53ee`…`b310d73` | 数据集验收工具 + Stage 1 PPO 核心 + Euler 奇异跳变修复 ×2 + 冒烟脚本加固 | — |
| 09-19 | `aa11b89`…`5052491` | G1 资产静态/运行时验收 + Stage 1 环境适配 + 代码路径对齐论文设计 + checkpoint 断点续训修复 | 764 |
| 09-20 | `7c46fb4`…`9309818` | **GPU9 物理路径验证** + 固定 warp 1.12.1 + G1 runner 可观测（`--metrics`）+ 单卡诊断处置 | 764 |

---

## 4. 已完成模块详解

### 4.1 数据管线（M1 / M1.5）✅

**流程**：LAFAN1 下载（77 个 BVH，~144 MB）→ BVH 解析 + FK → Mixamo 骨骼重定向到 G1 29-DoF → 全链 LM 位置 IK 精修 → 质量评估 + 产物验收。

**重定向实现中的数据驱动发现**（与预想不同，均已成文档）：

- LAFAN1 rig 是**左手系变体**（上=+x、前=+y、左=+z，zyx 序，cm 单位），绑定姿态不可信（腿折叠）；
- 源→G1 对齐矩阵是**与运动数据无关的常数**（G1 链基静止朝向 × 循环置换），而非逐帧求解；
- 根朝向换算 `root_rot = (qw ⊗ q_src) ⊗ Q_MRIG_INV` 抽成唯一出处 `source_root_quat_to_g1_base()`，左右乘不可交换，有真实数据闭环测试锁死；
- 躯干 18° 误差的根因是一次 **FK bug**（torso_link 的腰 pitch 关节映射缺失），修复后躯干朝向误差 0.0°；
- 万向节保护 `_gimbal_protect()`：3 铰链链中间角近 ±90° 时外角提取退化，奇异区用邻域好帧 (sin,cos) 插值；速度尖峰 0.31% → 0.17%；
- 倾斜链精确逆（FK 组合互逆）反而使 FK 位置误差变差（20.9→25.8 cm）——**负结果**，已回退并记录。

**质量指标（77 序列实测，`data/processed/quality_report.csv`）**：

| 类型 | n | fitted (cm) | holdout (cm) | upright (cm) | spike % | contact % |
|---|---|---|---|---|---|---|
| obstacle | 17 | 14.36 | 15.37 | 27.46 | 0.20 | 88.7 |
| walk | 12 | 13.79 | 14.81 | 26.02 | 0.08 | 86.4 |
| dance | 8 | 14.14 | 14.85 | 26.14 | 0.76 | 82.5 |
| push | 6 | 14.12 | 14.94 | 26.89 | 0.36 | 87.9 |
| fall | 6 | 14.77 | 15.90 | 34.54 | 0.43 | 73.0 |
| fight / aim / run / multiple / jump / sprint | 21 | 13.6–14.2 | 15.0–15.3 | 25–30 | 0.1–0.5 | 66–93 |
| **ground** | 5 | **16.31** | **16.28** | **38.46** | 0.57 | 81.7 |
| **总体** | **77** | **14.25** | **15.22** | **28.58** | — | — |

**指标口径（重要，防自证）**：`fitted_err_cm` 是 13 个 IK 优化点的**训练残差**（非独立）；`holdout_err_cm` 是 4 个未进 IK 目标的点（左右髋、左右趾），但与拟合点共享运动链，属**泛化检验**而非独立测量；曾用于裁定的 `root_rot_err_deg` 被证实是**恒等式**（按构造恒为 0.01°，无区分度），已从结论依据中剔除。

**几何地板**：G1 无脊柱（pelvis→torso_link 仅 4.4 cm）而 LAFAN1 Hips→Spine2 ≈31 cm，Spine2/双肩的系统误差任何关节角优化无法消除 → 原"≤12 cm"验收线不可达，改为"非 ground 类 ≤15 cm + 系统关键点单列"。

**产物验收**（`data/processed/acceptance_continuous_full.json`，77 序列全量）：496,672 帧、**16,555.6 s（约 4.6 小时）**运动；关节限位违例 **0**、qvel 差分失配 **0**、四元数范数误差 ≤1.2e-7、接触标签失配 **0**。

**遗留问题（A17 ground 类裁定——已复核，方向反转）**：旧裁定理由"ground 重定向退化 38–116 cm"在当前数据上**不成立**（现 16.28 cm，与 fall 类完全重叠）。09-20 完成两项复核后，裁定从"默认过滤 ground"**改判为"默认保留全部 77 序列，先修垂直锚定再确认"**：

1. **论文证据（引文已逐条核实）**：§IV-B 训练设置对数据只说 "motions from LAFAN1 retargeted to a humanoid robot. Motion segments are sampled across reference sequences and starting frames"，无序列数、无任何筛选条件；自适应采样描述明确 "retaining uniform coverage of the **full motion dataset**"；全文**没有任何数据过滤表述**。且论文的能力主张直接依赖躺地/摔倒类动作——§V-D 遥操作 "executes diverse operator-commanded behaviors, including punching, squatting, **lying down**, and recovering to a standing posture"；扰动实验 "recovers from the **fallen configuration** … robust whole-body control across both **upright and fallen states**"。摔倒经验虽也会经 recovery curriculum 的 post-fall 状态池进入训练，但"执行躺地参考"这一能力只有训练分布里存在躺地参考才能学到。
2. **`data.probe_heights` 实测（09-20，7 序列）**：锚定假设的失效机制已定位——锚定量取"全序列 min(足高−髋高)"，它选中的其实是**最接近站立姿态的帧**；躺地序列因此整体漂浮（ground1_subject1/4 的 G1 双踝原点**全段** ≥0.42/0.49 m，源动作平躺、参考悬浮半米），而跨障序列因髋部低于足部而被压进地面（obstacles5_subject2 踝 −0.286 m）。对照 walk1_subject1 正常（踝最低 0.054 m）。
3. **关键澄清：病根是锚定而不是 ground 类**。5 个 ground 序列中仅 2 个严重漂浮（ground1_subject1/4），其余 3 个踝高 0.078/−0.018/0.097 m 接近可用；反而 **14/17 个 obstacle 序列**存在低于名义平面的帧。按类过滤本来就打偏了目标。

**行动项已完成（09-20 晚）**：垂直锚定改为**两段式 v2**——① `retarget()` 源侧按"全序列全身最低体点触地"（数学上化简为 `root_z = hip_z − min(全身最低体点)`，锚定常数全部消去）；② `_finalize` 在 IK 后按 G1 自身 FK 的最低体点做刚体 z 校正（IK 目标随 root 刚体移动，残差与 qpos 不变；最低体点为踝时锚到静止踝高 0.036 m，否则锚到接触面 0）。修复实测（旧 → 新，G1 最低踝原点 z）：ground1_subject1 **+0.418 → +0.036**、ground1_subject4 **+0.493 → +0.046**、obstacles5_subject2 **−0.286 → +0.033**、walk1 对照 +0.054 → +0.036——躺地序列身体贴地（踝高均值 0.17–0.49 m），无任何穿地。**77 序列全集已重生成**于 `data/processed/lafan1_g1_anchored`：与上一版逐位对比确认 qpos 不变（Δ≤5e-7）、root z 为常量平移（最大 0.253 m，归因干净）；`validate_retarget` 零违例（496,672 帧，限位/差分/四元数/接触全过）；当前管线质量基线：拟合 14.80 cm / 留出 12.67 cm（`quality_report_anchored.csv`）。**训练自此使用 77 序列全集**；42 序列的 `lafan1_g1_continuous` 废弃。`probe_heights` 增加 `--npz-dir` 参数（此前硬编码读旧目录）。

**同场新发现（登记为新假设）**：论文 Fig. 3(b) 的评估协议存在 motion-group 分组（图注 "L9 motion-group success differences from PGMT-Ours (48 episodes per bar)"），960 个 L9 集 = 48/条 × 20 条，若条 = (组 × 4 消融变体) 则**恰为 5 组**——但论文全文未定义组名；对原图（内嵌 3600×4185 位图）三次独立识别，横轴标签均为**纯数字编号**，不是组名。因此"12 个 LAFAN1 前缀 → 5 组"的映射（影响 M5 基准与 A11 兼容规则的运动分类器）只能作为我方假设登记（拟 A23）。另 Fig. 4 图注 "truncated at 10k PPO iterations" 为**两阶段合计**的训练预算上限，阶段配比未公开，需一并登记。

### 4.2 观测契约与策略网络（M2.0）✅

维度常量唯一出处 `pgmt/contracts.py`：`OBS_DIM=96`、`ACT_DIM=29`、`HISTORY_LEN=10`、`REF_FRAME_DIM=61`。

- **观测 `o_t`（96 维）**：参考相对锚点朝向 6D `e_t`(6) + 基座角速度(3) + 关节位置(29) + 关节速度(29) + 上一动作(29)。线速度不进策略观测（属特权观测，只给 critic；参考平面速度经 `C^K` 进入）。
- **`e_t` 定义**：`R_rel = R_ref^T · R_robot`（参考锚点系下的机器人朝向）取 6D 表示——对全局旋转不变，与 root-centric 设计一致，有不变性测试。
- **历史 `H_t`（10×96）**：不含当前帧（`o_t` 已单独作 query）；冷启动前端填充，`e_t` 段填 6D 恒等旋转（6D 零向量不是合法旋转编码）；`observe_then_advance()` 把取值/推进顺序封装进方法，防调用方写反。
- **未来参考 `C^K`**：K=6，偏移 2^k−1 = 0/1/3/7/15/31 控制步（约 0.62 s 前瞻），每帧 61 维 = q^r(29)+q̇^r(29)+修正平面速度 ṽ^r(3)。
- **策略模块（全部有单测）**：RoPE（64 维）、MHCA（4 头 / 256 token）、History Encoder、IFM（MHCA+RoPE）、Terrain-Glimpse Encoder（Ng=4、5×5 patch、双线性可微采样、位置 tanh×1 m）、Actor MLP [512,256,128]、多头 Critic（共享 backbone，3 头/4 头切换）。
- **数值细节**：四元数最短弧 slerp（半球对齐，真实数据相邻步 <30° 验收）；非单位四元数先归一化（`quat_to_mat` 对尺度既非等变也非 Gram-Schmidt 可救——公式齐次项 `1` 不随尺度变，有反例守卫测试）。

### 4.3 奖励系统（M2.0b）✅

- **Table I 逐项权重照搬论文**（upper 6 / lower 6 / auxiliary 10 / terrain-contact 6 共 28 项），回归测试用论文表格的**字面量副本**逐项断言，抄错即失败；未知项名直接报错（防静默少算）。
- **Eq.10 地形感知松弛** `ẽ = [e − α·χ(κ)·τ(d)]₊`：χ 仅在 slope/stairs/boxes 激活，τ 随难度线性增长并饱和（≈各目标 5% 量级），非 TA 项恒 α=0（upper body 与速度项保持严格）。
- **核函数**：论文未明写，依 OmniH2O 奖励表与配置注释定为**高斯式 `exp(−e²/σ)`**（曾误记为线性核 `exp(−e/scale)`，两者不可重参数化互换，已有测试锁定）；σ 表区分上下半身（upper σ 更小 ↔ "上半身保姿态、下半身让位平衡"）。**σ 最终标定须等训练读到真实误差分布**，`data/probe_reward_scales` 探针只能暴露量纲错误与响应区。
- **符号纪律**：所有残差恒 ≥0，正负只由权重携带；`RewardGroup.sum` 运行时拒绝负值/NaN——防"惩罚项负值 × 负权重 = 正贡献"这类训练曲线上极难定位的反转错误。
- **语义对照表**（`semantics.py`，28 项）：IDENTICAL 11 / APPROX 7 / PGMT-SPECIFIC 6 / **UNRESOLVED 4**（`head_torso_impact`、`ee_accel_mismatch`、`floating_anchor_pos`、`ta_link_ori`——论文只给项名与权重，已按显式假设实现，参照依据待核对）。标注纪律由测试强制：note 含"未确认/未验证"只能标 UNRESOLVED。
- **已知数据源差异**：`reference_contact_match` 的参考接触标签，论文取自 offline terrain-mesh queries，本仓库取足部速度阈值——不同源，该项数值有系统性偏差（最终报告需说明）。

### 4.4 地形系统（M3，纯逻辑）✅

- **5 族 × L0–L9 解析高度场**（`terrain/generators.py`，A7/A19）：slope 5°→30°、stairs 阶高 4→24 cm、boxes 5→40 cm（对齐真机 37 cm 上限）、rough 幅值 1→12 cm、flat=随机化递增；tile 边长 >2 m（保证一张高程图不跨 tile）+ 四周留白（跨 tile 高度连续）。
- **21×21 yaw 对齐高程图**（`elevation_map.py`）：2 m×2 m、分辨率 2/(n−1)（与 map_size 非独立参数），存**相对基座**高度；含 A8 高斯噪声 + 随机 dropout 扰动；与 Glimpse Encoder 的采样坐标约定逐位一致（含转置检查——对称测试抓不到的错位）。
- **课程**（`curriculum.py`）：每环境独立状态机，只调 level 不调 family；连续成功升级、失败清零。
- **兼容规则**（A11）：运动类别（LAFAN1 序列名前缀，12 类）× 族/难度排除表，未分类运动**保守放行**。
- 验收：解析对照（slope rise=inner·tanθ、stairs 台阶索引、boxes 只取两个高度值）+ 生成高程图喂真实 Glimpse Encoder 采样值逐点一致 + 课程转移表穷举。可视化：`eval/viz/terrain_overview.png`。

### 4.5 训练系统（M2/M4 框架层）✅ 接口闭环

- **入口**：`pgmt/train/train_stage1.py` / `train_stage2.py`，`--backend torch|isaaclab|mock`。
  - `torch` 后端 = URDF FK + 真实 body-level tracking residual + PD + A13 位置修正 + 批量 PPO，**不含刚体动力学**（协议回归基线）；
  - `isaaclab` 后端 = 真实物理，强制要求 `--asset/--urdf/--reference-data`，不会静默退回 torch。
- **多头 PPO**：分头回报 / 分头 GAE / 分头 value loss（Stage 1 三头 upper/lower/aux，Stage 2 四头 +terrain）；checkpoint 保存网络 + Torch 环境完整状态（参考帧、观测历史、接触历史、恢复池、自适应采样状态），`--updates` 为总迭代目标，恢复时只跑剩余迭代。
- **论文机制接入**：recovery curriculum（摔倒状态池 + 生存率渐进）、adaptive sampling（段权重 = 1+β·失败次数，权重下限 1 保全覆盖）、global position correction（smoothstep 门控 + λ_pos + clip）。
- **可观测性**：`--metrics` 把最终更新统计与设备信息写 JSON——对策是 Isaac Kit 关闭时会吞掉终端输出（实测踩坑）。

### 4.6 仿真器落地与 GPU9 物理验证（M0 收口，09-20）✅

验证批次：`runs/validation_20260920_gpu9/`（当天完成，物理卡 `--device cuda:9`）。

| # | 验证项 | 结果 |
|---|---|---|
| 1 | G1 资产静态验收（`check_g1_asset.py`） | URDF/MJCF 的 29 关节、29 执行器、body 名、网格路径核验通过；manifest 含 sha256 与许可路径（ProtoMotions v2.3 授权资产） |
| 2 | **USD 运行时探针**（`probe_g1_isaaclab.py --device cuda:9`） | Isaac Sim 进程内读到 29 关节 / 33 body；USD 关节顺序 ≠ URDF 顺序，`joint_ids_canonical` 完成映射（`g1_usd_probe.json`，ok=true） |
| 3 | **箱体物理冒烟**（`smoke_test_isaaclab.py`） | 20 步 headless 物理，箱体静止高度 z=0.250 恒定，PhysX 正常 |
| 4 | **CPU/GPU 数值合约**（`check_cuda_contracts.py`） | Stage1/Stage2 策略前向 CPU↔CUDA 最大差 ≤3e-8；奖励分组差 0；地形高程图 finite 且差 0；参考采样差 ≤6e-4、根角速度误差 ≤1.5e-3（RTX 5880 Ada 上） |
| 5 | Torch CUDA 最小闭环 | Stage 1（三头，checkpoint 17 MB，reward_mean 4.40）与 Stage 2（四头，25 MB）各 1 次 PPO 更新成功 |
| 6 | **Isaac Lab 真实 G1 最小 PPO 更新** | 5 次尝试（warp1121a–e）：c 为 `/tmp` 日志权限错误、d 为 prim path 非全局（代码修复：`7986abb`）；**e 成功**——`--num-envs 2 --steps-per-env 2 --updates 1`，cuda:9，保存 17 MiB checkpoint（`stage1_isaaclab_warp1121e.pt`）。场景创建 1.74 s、仿真启动 0.84 s |

**平台问题归因（多卡共享服务器）**：

- 主机启用 IOMMU，Isaac Sim 启动时 `carb.cudainterop` 对全部 10 卡做 P2P 探测，打印 `cudaErrorTooManyPeers`——**启动诊断噪音，不阻塞单卡物理**（上述 2/3/6 项均在该输出存在下通过）。
- 处置：显式 `multi_gpu=False` + `renderer.multiGpu.maxGpuCount=1`（限制渲染路径）；**不用** `CUDA_VISIBLE_DEVICES=9` 隔离（实测会使 GPU foundation 进 CUDA bad state、冒烟失败），正式路径 = 不设变量 + `--device cuda:9`。全局拓扑探测需管理员级 IOMMU/驱动调整，不在本项目权限内。
- **warp 版本坑**：Isaac Sim 5.x Python 扩展依赖 `warp.types.array`，新版 warp（1.14+）删除了该属性 → 固定 `warp-lang==1.12.1`。

### 4.7 PPO 可观测性与优化配置缺陷（09-20 GPU8 中配验证发现，已修复）

256 env × 24 步 × 50 updates 的中配验证（GPU8）暴露出"actor 冻结"（approx_kl 恒 ≤1.2e-8、clip_fraction 精确为 0）与"value loss 爆炸"（6.99e4 → 1.2e7 振荡）两个症状。经隔离实验（合成 rollout / torch 后端真实数据 / Isaac Lab 真实物理三轮对照）定位为**同一个根因**：

- **根因**：`train_stage1.py` / `train_stage2.py` 的 CLI 默认值 `--learning-epochs 1 --mini-batches 1`。README 的正式训练命令未显式传这两个参数，于是每次 update 只有一个巨型 minibatch、每 6144 样本的 rollout 只走 **1 步梯度**（A6 规格为 5 epochs × 4 minibatches = 20 步，实际只做了 1/20 的优化工作量）。
- **症状一（观测全盲，非训练停止）**：PPO 的 ratio 指标在每个 minibatch 的 `optimizer.step()` **之前**求值；单一 minibatch 时参数与采样时完全相同，ratio ≡ 1，故 approx_kl / policy_loss / clip_fraction 是恒等值——"actor 冻结"是测量伪影。隔离实验证明 PPO 核心在各奖励尺度下均健康（actor 单次 update 位移 ~2.6，三头 advantage 归一化后无抵消，相关矩阵正常）。
- **症状二（真实缺陷）**：critic 每 rollout 只被训 1 步，跟不上 aux 头量级 ~−450 的回报，value loss 剧烈振荡。
- **修复**（未提交，测试 765 全绿）：① 两入口的 epochs/mini-batches 默认值改为 `None` → 回退 A6 假设表（单一出处）；② `ppo.py` 新增 `approx_kl_post` 指标——在更新后参数下重估同一批数据，单一巨批配置不再对策略更新失明；③ `tests/test_train.py` 新增回归测试锁定该教训。
- **端到端确认**（GPU8，同配置 + 5×4，50 updates）：approx_kl 0.22 → 随 lr 退火降至 ~1e-3（健康），clip_fraction 活跃，value loss 稳定在 ~5e4（爆炸消失），熵稳定于 42.18；reward_mean 50 updates 内持平（−42 → −46，该规模下属预期，不代表收敛）。吞吐 ~10.7 s/iter @ 256 envs（PPO 计算占比小，rollout/物理主导）。
- **正式训练观察项**：update 1 的 clip_fraction 0.71 偏高（理想 <0.2），需注意早期 lr/优势尺度 interplay；aux 回报尺度大（−450）但稳定，是否引入 value 归一化留作正式训练时决策（改动会偏离论文规格，先观察）。

---

## 5. 质量保证体系

1. **回归测试全绿**（09-20 晚实测，**767 passed** = 原 764 + PPO 可观测性回归 1 + v2 锚定回归 2；其中约 55 项 data-gated，需真实 LAFAN1 数据，缺失自动 skip）。覆盖：策略模块逐个、观测契约、奖励规格字面量对照、地形解析/闭环、终止条件、重定向/IK/评估口径、训练集成、消融合同等。
2. **无仿真器集成冒烟**：真实重定向序列跑通"运动库 → C^K → H_t → o_t → HistoryEncoder/IFM/Glimpse/Actor/四头 critic"全链路（含梯度），在搭环境前就能发现接口错配。
3. **指标独立性审计**（数据管线三轮回审）：识别并处置了自证指标（fk_err 与 IK 目标重叠）、恒等式指标（root_rot_err_deg）、伪独立留出点（共享运动链）——每处都有测试把口径锁死，防止未来回退。
4. **静默错误防线**：惩罚值域守卫、未知奖励项名报错、HistoryBuffer 输入复制（防观测历史被复用缓冲篡改）、`e_t` 输入归一化、语义标注纪律测试。
5. **产物完整性门禁**：`data/validate_retarget.py`（限位/差分/四元数/接触一致），报告写入用 `.partial.csv` + 原子替换，失败不覆盖正式报告。

---

## 6. 与论文的对照：当前位置

```
论文流水线:  数据 → Stage1 平地训练 → Stage2 感知训练 → 9600 集评估 → 消融/基线
本仓库:      数据 ✅ → 策略/奖励/地形/训练框架 ✅ → 物理接口 ✅(最小闭环) → [训练 ▶ 未开始]
```

**已具备**：跑 Stage 1 正式训练的全部前置条件（资产、参考数据、环境适配器、PPO、断点续训、metrics 落盘）。

**尚不能声称的**（防误读，README 反复强调）：
- 最小 PPO 更新（2 env × 2 步 × 1 次）只证明**启动与接口闭环**，不代表训练收敛，更不代表论文指标；
- `--backend torch` 结果不含刚体动力学，不能替代物理仿真验收；
- Table II / Fig.3 / Fig.4 的**任何数字尚未复现**。

---

## 7. 已知问题与风险清单

| # | 问题/风险 | 现状与对策 |
|---|---|---|
| 1 | ~~A17 ground 类过滤待复核~~ **已裁定（09-20）**：论文证据支持保留全集；病根是垂直锚定（躺地漂浮/跨障穿地），且 obstacle 类同样受累 | 修锚定 → 77 序列全集重生成（新目录）→ 再训练；42 序列目录废弃；短期修不了才只排除实测漂浮的 2 序列并记为偏差 |
| 2 | 奖励 σ 未标定（探针只量参考幅度，非跟踪误差） | 等训练读到真实误差分布后重跑探针对照再定 |
| 3 | 4 项奖励 UNRESOLVED（论文只给项名/权重） | 已按显式假设实现并登记；参照依据待核对 |
| 4 | `reference_contact_match` 接触标签不同源（速度阈值 vs 论文 terrain-mesh queries） | 系统性偏差，最终报告说明 |
| 5 | Stage 2 地形物理未接入（heightfield/trimesh 注入 Isaac Lab、接触传感器） | 下一阶段核心工作 |
| 6 | IOMMU/P2P 启动噪音 | 已归因为诊断输出；单卡验证不受影响；不作为失败条件 |
| 7 | 论文 15k envs / 8.44 s/iter 的吞吐在本机未实测 | Stage 1 扩规模时回填 Time/VRAM/Envs 实测列 |
| 8 | RGMT-Reimpl 基线工作量 | 排 P2，先保主结果 |

---

## 8. 下一步计划（按优先级）

1. ~~修垂直锚定并全集重生成~~ **已完成（09-20 晚）**：两段式 v2 锚定上线，77 序列全集重生成并通过验收（§4.1），训练参考集切换为 `data/processed/lafan1_g1_anchored`。
2. **Stage 1 扩规模训练**（P0）：PPO 配置缺陷已修复并通过端到端确认（§4.7），阻塞解除。按论文量级放大（15k envs、两阶段合计 ≤10k iter，Fig.4 截断点；阶段配比登记为假设），用 `--metrics` 记录奖励分组、终止原因与 GPU 吞吐（关注 §4.7 的两个观察项：早期 clip_fraction、aux value 尺度）；同配置 `--backend torch` 结果留作接口回归基线。验收目标（M2）：平地 completion >90%，body pos RMSE <0.1 m 量级。
3. **登记新假设 A23**（motion-group 映射）：12 个 LAFAN1 前缀 → 论文 Fig.3(b) 的运动分组（算术上为 5 组、组名未公开）；同时影响 M5 基准数据集与 A11 兼容规则分类器。
4. **Stage 2 地形物理接入**（P0）：terrain atlas 高度场注入 Isaac Lab 碰撞场景，验证接触传感器、21×21 elevation、terrain-contact 六项奖励、四头 critic。
5. **σ 标定**：用 Stage 1 实测误差分布对照探针响应区，必要时调整 `spec.SIGMAS`。
6. **M5 评估框架**：9600 matched episodes（种子固定，按 A23 分组）+ Wilson CI 指标 + 4 消融 + Aggregate-critic 变体（共享 Stage 1 checkpoint，10 卡运行矩阵见 `复现方案.md` §6）。
7. **M6**：RGMT-Reimpl 基线（尽力而为）+ 最终对标报告（含 A1–A23 逐项偏差说明）。

---

## 附录 A：关键命令

```bash
# 回归测试（服务器环境）
/data/jxc/envs/pbfm_isaaclab/bin/python -m pytest tests -q     # 764 passed

# 数据管线（原始数据不入库，需自行生成）
bash data/download_lafan1.sh
python -m data.retarget_lafan1 --bvh-dir data/raw/lafan1 --out-dir data/processed/lafan1_g1
python -m data.eval_retarget && python -m data.validate_retarget

# G1 资产验收（USD 需在 Isaac Sim 进程内核验）
python setup/probe_g1_isaaclab.py --device cuda:9 --asset <g1.usd> --json data/processed/g1_usd_runtime_probe.json

# 物理冒烟
python setup/smoke_test_isaaclab.py --device cuda:9 --steps 20

# Stage 1 正式训练（Isaac Lab 真实物理，单卡 cuda:9）
python -m pgmt.train.train_stage1 --backend isaaclab --device cuda:9 \
  --num-envs 256 --steps-per-env 24 --updates 1000 \
  --asset <g1.usd> --urdf <g1.urdf> \
  --reference-data data/processed/lafan1_g1_anchored \
  --checkpoint runs/stage1_g1_isaaclab.pt --metrics runs/stage1_g1_isaaclab.metrics.json

# Stage 2 协议路径（无 Isaac Sim 时验证接口）
CUDA_VISIBLE_DEVICES=0 python -m pgmt.train.train_stage2 --backend torch --device cuda:0 \
  --reference-data data/processed/lafan1_g1_anchored --urdf <g1.urdf> \
  --num-envs 4 --steps-per-env 24 --updates 1
```

## 附录 B：关键产物索引

| 产物 | 路径 |
|---|---|
| 质量报告（77 序列，当前管线） | `data/processed/quality_report_anchored.csv`（v2 锚定；拟合 14.80 / 留出 12.67cm） |
| 产物验收 JSON（v2 锚定） | `data/processed/acceptance_anchored.json` |
| **正式训练参考集** | `data/processed/lafan1_g1_anchored`（77 序列全集，v2 两段式锚定）；旧目录（`lafan1_g1` / `_continuous` / `_continuous_full`）保留对比、不用于训练 |
| G1 资产 manifest | `data/processed/g1_asset_manifest.json` |
| USD 运行时探针 | `runs/validation_20260920_gpu9/g1_usd_probe.json` |
| GPU9 验证日志与 checkpoint | `runs/validation_20260920_gpu9/`（isaaclab_smoke.log、cuda_contracts_after_fkfix2.log、stage1_isaaclab_warp1121e.pt 等） |
| 地形可视化 | `eval/viz/terrain_overview.png`、重定向对比图 walk/dance/jumps/ground |
| 假设清单 A1–A22 | `pgmt/cfg/assumptions.py`（训练时 `dump()` 写入日志） |
| 复现方案（含验收标准/风险全表） | `复现方案.md` |

## 附录 C：假设体系摘要（论文未公开细节的显式登记）

A1 控制频率 50 Hz · A2 K=6 未来帧（偏移 2^k−1） · A3/A4 模型规模（token 256 / MLP [512,256,128]，按 OmniH2O/HOVER） · A5 RoPE 64 · A6 PPO（clip 0.2 / γ 0.99 / λ 0.95 / lr 1e-3 递减 / 24 步 horizon） · A7 地形难度端点（boxes L9=40 cm 对齐真机 37 cm） · A8 高程图噪声 · A9 特权观测集（48 维表） · A10 摔倒池 · A11 兼容规则 · A12 松弛参数（α=1、τ 饱和 5% 量级） · A13 速度门控/修正增益 · A14 重定向 · A15 Glimpse MLP 规模 · A16 自适应采样（线性软加权） · A17 ground 过滤（**已改判：默认保留全集，锚定修复后确认**） · A18 奖励实现（核 exp(−e²/σ)、σ 表、τ 斜率） · A19 tile 规格 · A20 终止条件三类触发+容忍区 · A21 auxiliary 组度量阈值 · A22 terrain-contact 组度量阈值 · **A23（拟新增）** motion-group 映射（12 前缀 → 5 组，组名论文未公开）与两阶段迭代配比（合计 ≤10k）。
