# PGMT 复现方案（Reproduction Plan）

> 论文：PGMT: Perceptive General Motion Tracking for Humanoid Robots（arXiv:2609.08511v2，2026-09-09）
> 项目主页：https://luyili.github.io/pgmt/ （截至方案制定时**官方代码未开源**，需自行实现）

## 0. 目标与范围

| 决策项 | 结论 |
|---|---|
| 复现保真度 | **完整对标**：Table II 主表 + Fig. 3 消融 + Fig. 4 critic 对比 |
| 范围 | **纯仿真**（Isaac Gym），不做真机部署、teleoperation、motion matching、LiDAR 建图 |
| 开发环境 | 本机 Windows：写代码 + 纯逻辑单元测试 + 环境脚本；**无法本地跑 Isaac Gym**（torch CPU） |
| 训练环境 | 用户服务器：**10× RTX 5880 Ada**（sm_89，48GB/卡，Linux），驱动 580 / CUDA 13.0；每卡独立跑一个 run（`CUDA_VISIBLE_DEVICES=k`），不做 run 内多卡。python 3.9 + torch 2.1.2 cu121 + rsl-rl-lib 2.1.2（PP4 官方绑定要求旧 torch ABI） |
| 基线 | 必做：PGMT-Pretrain、4 个感知消融（NoHeight/CNN/2Glimpse/Fixed）、Aggregate-critic；尽力而为：RGMT-Reimpl、SONIC v1.1 External |

## 1. 论文实现规格摘要

### 1.1 机器人与动作
- Unitree G1，29-DoF；动作 `a_t ∈ R^29` = 关节位置目标，经 PD 控制器转力矩
- 控制频率假设 **50 Hz**（论文未明说，按 G1 惯例，记为假设 A1）

### 1.2 观测空间
- `o_t ∈ R^96` = [参考相对 anchor 朝向 `e_t`(6, 6D 旋转), 基座角速度 `ω_t`(3), 关节位置 `q_t`(29), 关节速度 `q̇_t`(29), 上一动作 `a_{t-1}`(29)]
- `H_t ∈ R^{10×96}`：10 帧 proprioception 历史
- `C_t^K`：未来参考帧，偏移 `2^k − 1`（k=0..K−1）以控制步计；访问 30 fps 数据时换算为 `offset × control_dt / source_dt`，每帧 61 维 = [`q^r`(29), `q̇^r`(29), `v^r`(3, 修正后平面 anchor 速度)]
- `M_t ∈ R^{21×21}`：yaw 对齐 2m×2m 高程图（仅 Stage 2 引入）

### 1.3 策略架构
```
Stage 1（平地预训练）                    Stage 2（感知注入）
o_t ──Q──┐                              o_t ──Q──┐
H_t ──KV─┴→ History Encoder(MHCA) → s_hist       │
              │                                   │
C^K ──KV──────┴→ IFM(MHCA+RoPE) → s_int ←──┴──── 地形 token z_1..z_4
                     │
        ┌────────────┴────────────┐
   Actor: MLP([o_t, s_int]) → a_t    Critic: MLP(o_priv, s_int) → V_upper/V_lower/V_aux(/V_terrain)
```
- **Terrain-Glimpse Encoder**（Stage 2 新增）：
  1. 位置选择器 `MLP_φ(vec(M_t), s_hist, C^K)` → 4 个 glimpse 位置 `r_j ∈ R^2`
  2. 双线性采样裁剪 5×5 patch（可微，无需落足点标注）
  3. `MLP_ψ(patch, ρ_j)` → 地形 token `z_j`
  4. 注入 IFM：`K = V = [s_hist; C^K; z_1..z_4]`
- **Multi-head critic**：共享 backbone，Stage 1 三头（upper/lower/aux），Stage 2 改末层为四头（+terrain）

### 1.4 奖励（Table I，照搬）
| 组 | 项（权重） |
|---|---|
| Upper body | link pos(1.0), link ori(1.0), link lin vel(0.5), link ang vel(0.5), joint pos(1.0), joint vel(0.5) |
| Lower body | TA link pos(0.5), TA link ori(2.0), link lin vel(0.5), link ang vel(0.5), TA joint pos(0.5), joint vel(0.5) |
| Auxiliary | root ori(0.5), corrected root vel(2.0), floating-anchor pos(1.0), recovery upward vel(12.5), pelvis vert accel(−1e-3), EE accel mismatch(−1e-3), action rate(−0.05), joint limit(−15.0), undesired contact(−0.1), head/torso impact(−1e-5) |
| Terrain-contact（仅 Stage 2） | touchdown quality(10.0), reference contact match(1.5), slip(−1.0), stumble(−20.0), contact switching(−30.0), contact force(−1e-6) |

### 1.5 两阶段训练机制
- **Stage 1**：平地；root-centric tracking（误差相对参考根 anchor 计算，不依赖世界系绝对位姿）；recovery curriculum（离线摔倒状态池，随 episode 生存率渐进提高池初始化比例）；adaptive sampling（跟踪失败频繁的运动段提高采样概率，保持全覆盖）
- **Stage 2**：加载 Stage 1 权重，注入感知。训练级调整：
  - **Terrain-aware tracking relaxation**：`ẽ = [e − α·χ(κ)·τ(d)]₊`，仅作用于 lower-body link pos / link ori / joint pos 目标；`χ(κ)` 仅在 slope/stairs/boxes 激活；`τ(d)` 随难度线性增长并饱和；`α_{h,j}` 逐元素缩放
  - **Global position correction**：`v^r = v^r + clip(g(‖v^r‖)·λ_pos·e^p)`，平面位置误差经参考系旋转后反馈到速度指令，静止时门控 `g(·)` 归零
  - **Terrain curriculum**：flat/slopes/stairs/boxes/random rough 五族 × L0–L9，level-based 升降级；behavior–terrain 兼容规则排除明显不可行组合

### 1.6 评估协议
- **9600 个 matched 30-s episodes** = 5 地形族 × 10 难度 × 192 集；各策略使用完全相同的运动参考、起始帧、地形分配、动力学随机化、观测扰动、执行器延迟
- 指标：Completion %、L9 %、body pos RMSE(m)、body ori RMSE(°)、joint RMSE(rad)、contact F1 + Wilson 95% 置信区间（Fig. 3）
- 训练成本列（Time/iter、VRAM、Envs/GPU）用我们自己的实测值

## 2. 关键假设清单（论文未公开的细节，需拍定并记录）

| # | 假设 | 默认取值 | 备注 |
|---|---|---|---|
| A1 | 控制频率 | 50 Hz（dt=0.02s） | G1 惯例；30s=1500 步 |
| A2 | 未来参考帧数 K | 6（偏移 0,1,3,7,15,31） | 论文只给公式未给 K |
| A3 | token 维度 / MHCA 头数 | 256 / 4 | 按 OmniH2O/HOVER 规模 |
| A4 | Actor/Critic MLP | [512,256,128] | 同上 |
| A5 | RoPE 维度 | 64（每头 64） | 常规设置 |
| A6 | PPO 超参 | clip 0.2, γ=0.99, λ=0.95, 学习率 1e-3 递减, 24-step horizon | 按 rsl-rl 惯例 |
| A7 | 地形难度参数（每族 L0→L9） | slope 5°→30°；stairs 阶高 4→24cm；boxes 高 5→40cm；rough 幅值 1→12cm；flat 难度=随机化强度递增 | 论文未给数值；40cm 对齐真机"37cm"上限 |
| A8 | 高程图噪声模型 | 高斯噪声(σ 随难度) + 随机 dropout 网格 | 论文提到 observation corruption 未给细节 |
| A9 | privileged 观测集 | 基座线速度、足端接触状态、摩擦、地形高度@足端、质量扰动等（Isaac Gym 标准集） | 论文未列 |
| A10 | 摔倒状态池采集方式 | 训练时在 termination（摔倒）时刻记录 `[o_t, 参考上下文]` 存入环形池 | 沿 RGMT 思路 |
| A11 | 兼容规则实现 | 运动特征分类器（躺/趴/倒立类不采样于 stairs/boxes 高级别；坐姿类不采样于斜坡高级别） | 论文只说是"粗规则" |
| A12 | 松弛参数 | `α_{h,j}=1`（lower-body 元素）、`χ` 按地形族、`τ(d)` 为难度线性+饱和函数（饱和值=各目标 5% 量级） | 论文只给形式 |
| A13 | 速度门控与修正增益 | `g(‖v‖)=smoothstep(0, 0.5, ‖v‖)`，`λ_pos≈1.0`，clip `v̄=1.0 m/s` | 论文只给形式 |
| A14 | 重定向管线 | Mixamo(LAFAN1) → G1：骨骼缩放 + 逐关节旋转映射，参考速度由有限差分 | 沿用 OmniH2O 思路 |
| A15 | Glimpse 编码器结构 | Ng=4、patch 5×5（论文给定）；MLP_φ 隐层 (512,256)、MLP_ψ 隐层 (256,)、位置输出 tanh×1.0m | 论文只给数量与 patch 尺寸；MLP 规模按 A4 风格 |
| A16 | Adaptive sampling | 段权重 = 1 + β·失败次数，β=1.0（软加权，权重下限 1 保留全覆盖） | 论文只说失败频次提高概率并保留全覆盖 |

> 所有假设在代码中集中于 `pgmt/cfg/assumptions.py`，训练日志记录实际取值，最终报告中与论文偏差单独成节。

## 3. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 仿真器 | **Isaac Gym Preview 4** | 用户指定；OmniH2O/PHC 等 tracking 代码成熟参考 |
| RL 框架 | rsl-rl（PPO） | 论文用 PPO；与 Isaac Gym 生态配套 |
| 基础代码参考 | **human2humanoid (OmniH2O)**：G1 资产、环境框架、重定向思路 | PHC 为备选；PGMT Stage 1 本质是 HOVER/OmniH2O 式 tracking + 论文新组件 |
| 数据 | **LAFAN1**（Ubisoft LaForge，GitHub 下载，研究用途免费） | 论文指定；Mixamo 骨骼 BVH |
| 本地开发环境 | Windows + conda（torch 2.1.2 CPU + numpy + pytest） | 只做逻辑单测与静态检查；torch 版本与服务器对齐以避免数值漂移 |

### ⚠️ 前置风险：Isaac Gym PP4 × RTX 5880 Ada（sm_89）× 驱动 580
- Isaac Gym Preview 4 是 cu11.8 时代产物：官方 Python 绑定仅到 **3.8/3.9**，且按旧 torch ABI 编译 → **torch 必须用 2.1 时代版本（cu121）**
- **好消息**：sm_89（Ada）正是 PP4 的社区黄金架构（RTX 4090 同代），legged_gym / OmniH2O / PHC 生态都在此架构上验证过
- **唯一未验证项**：驱动 **580** 超出社区黄金线（525/535），但 headless 训练在 555+ 有先例
- **M0 必须在服务器上先做兼容性冒烟测试**，决策树：
  1. 官方 PP4 + python 3.9 + torch 2.1.2 cu121 → 若可运行 ✅（主路线）
  2. 驱动 580 导致 `create_sim`/PhysX 崩溃 → 降驱动到 535/545 档
  3. 兜底：**转 Isaac Lab**（官方支持 Ada/Blackwell + 新驱动 + cu128，python 3.11 + torch 2.7）。代码按"环境层与策略层解耦"设计，迁移成本可控
- 冒烟脚本：`setup/check_isaacgym.sh <tar>`（1 分钟静态检查）→ `setup/install_server_isaacgym.sh --isaacgym <tar>` → `python setup/smoke_test.py`

## 4. 仓库结构

下图保留早期规划的文件名与里程碑标记；当前已实现内容以 README 和实际源码为准。松弛逻辑现位于 `rewards/spec.py`，奖励、终止和地形纯逻辑已实现，环境与训练入口仍待接入。

```
D:\PGMT\
├── 复现方案.md                # 本方案
├── README.md                  # 仓库说明 + 快速开始
├── 2609.08511v2.pdf           # 论文原文（仅本地参考，不纳入版本控制）
├── out.txt                    # 论文纯文本抽取（仅本地参考，不纳入版本控制）
├── setup/
│   ├── requirements.txt       # 服务器 Python 依赖（PP4 路线，版本锁定）
│   ├── requirements-dev.txt   # 本机开发依赖（纯逻辑单测）
│   ├── check_isaacgym.sh      # PP4 包静态检查（绑定 Python / PhysX 架构）
│   ├── install_server_isaacgym.sh  # 主路线：官方 PP4 + Ada（py3.9 + torch 2.1.2 cu121）
│   ├── install_server.sh      # 兜底：Isaac Lab（py3.11 + torch 2.7 cu128）
│   ├── install_local.bat      # 本机 Windows 开发环境
│   ├── smoke_test.py          # 服务器冒烟（PP4 路线，M0）
│   └── smoke_test_isaaclab.py # 服务器冒烟（Isaac Lab 路线）
├── pgmt/
│   ├── cfg/
│   │   ├── assumptions.py     # 假设清单 A1-A22 的唯一出处
│   │   ├── ppo_cfg.py         # rsl-rl 配置          【M2 未实现】
│   │   └── env_cfg.py         # 环境/随机化/课程配置  【M2 未实现】
│   ├── envs/
│   │   ├── g1_env.py          # G1 29-DoF 环境（观测/动作/PD）  【M2 未实现】
│   │   ├── terrain/           # 【M3 未实现】
│   │   │   ├── generators.py  # 5 族 × L0-L9 地形生成（解析函数）
│   │   │   ├── elevation_map.py  # 21×21 yaw 对齐高程图 + 噪声
│   │   │   └── curriculum.py  # level-based 课程 + 兼容规则
│   │   ├── reference_sampler.py  # ✅ C^K 采样 + A13 修正速度 + A16 自适应采样
│   │   └── recovery_pool.py   # 摔倒状态池  【M2 未实现】
│   ├── policy/                # ✅ 全部已实现并有单测
│   │   ├── history_encoder.py # MHCA(s_hist)
│   │   ├── ifm.py             # MHCA + RoPE（s_int）
│   │   ├── glimpse_encoder.py # 位置选择器 + 双线性采样 + patch MLP
│   │   ├── rope.py            # Rotary Position Embedding
│   │   ├── mhca.py            # 共享 MHCA 层（可选 RoPE）
│   │   ├── mlp.py             # 共享 MLP 构造器（ELU）
│   │   ├── actor.py
│   │   └── multi_head_critic.py  # 3/4 头共享 backbone
│   ├── rewards/               # 【M2/M4 未实现】
│   │   ├── tracking.py        # upper/lower（root-centric）+ TA 版本
│   │   ├── auxiliary.py       # root/恢复/正则化
│   │   ├── terrain_contact.py # Stage 2 接触组
│   │   └── relaxation.py      # 地形感知松弛
│   ├── train/                 # 【M2/M4 未实现】
│   │   ├── train_stage1.py    # Stage 1 入口
│   │   └── train_stage2.py    # Stage 2 入口（加载 stage1 ckpt）
│   └── utils/                 # 【未实现】日志、buffer、接触标签工具
├── data/                      # ✅ M1 + M1.5 全部完成
│   ├── download_lafan1.sh     # LAFAN1 下载（GitHub LFS media 地址）
│   ├── bvh.py                 # BVH 解析 + FK
│   ├── retarget_lafan1.py     # Mixamo→G1 重定向（A14）+ 批量导出
│   ├── ik_refine.py           # 批量 LM 位置 IK（M1.5）
│   ├── eval_retarget.py       # 质量评估 → data/processed/quality_report.csv
│   ├── viz_retarget.py        # 源/G1 并排可视化
│   ├── raw/lafan1/*.bvh       # 77 个源序列（gitignore，可重下载）
│   └── processed/
│       ├── lafan1_g1/*.npz    # 77 个精修序列（**不入库**：体积 + LAFAN1 衍生数据许可，见 NOTICE.md）
│       ├── lafan1_g1_v1_snapshot/  # 旧版快照，对比用（不入库）
│       ├── quality_report.csv # 质量报告（入库）
│       └── viz/*.png
├── eval/                      # 【M5 未实现】仅 eval/viz/*.png（M1.5 可视化）
│   ├── benchmark_dataset.py   # 9600 episodes 匹配数据集（种子固定、可复现）
│   ├── run_eval.py            # 策略评估入口
│   ├── metrics.py             # completion/RMSE/F1/Wilson CI
│   └── ablations/             # NoHeight/CNN/2Glimpse/Fixed/Aggregate-critic 变体配置
├── baselines/
│   └── rgmt_reimpl/           # RGMT 复现（尽力而为，M6）
└── tests/                     # 82 个测试函数（15 个 data-gated）
    ├── test_rope.py / test_mhca.py / test_history_encoder.py
    ├── test_ifm.py / test_glimpse.py / test_actor_critic.py
    ├── test_policy_integration.py / test_assumptions.py
    ├── test_reference.py / test_bvh.py
    ├── test_retarget.py / test_ik_refine.py / test_viz.py   # 需 data/raw/lafan1
    └── test_relaxation.py / test_terrain.py / test_rewards.py  【M2/M3 待补】
```

## 5. 里程碑

> **当前状态速览**（公开仓库读者请先看这里）
>
> | 区域 | 状态 |
> |---|---|
> | M1 / M1.5 数据管线（下载→重定向→IK 精修→质量评估） | ✅ 完成；本轮全套 728 通过，2 CUDA 跳过 |
> | M2.0 观测契约（`contracts.py` / `observations.py` / `rotation.py`） | ✅ 完成（纯逻辑，不依赖仿真器） |
> | M2.0b 奖励规格（Table I 权重 + Eq.10 松弛 + 语义对照表） | ✅ 完成（纯逻辑） |
> | 策略网络（RoPE / MHCA / History Encoder / IFM / Glimpse / Actor / 多头 Critic） | ✅ 完成（有单测） |
> | M3 地形系统（5 族 × L0–L9 高度场 / 高程图 / 课程 / 兼容规则） | ✅ 完成（纯逻辑；mesh 注入归 M2） |
> | 奖励残差实现（Table I 全部 28 项）+ 终止条件 | ✅ 完成（A20 / A21 / A22） |
> | 训练入口 / 多头 PPO / G1 环境 | ⬜ **未实现**（M2） |
> | 基准评估与消融 / 基线 | ⬜ 未实现（M5–M6） |
>
> 也就是说：**"不依赖 Isaac Gym 的部分"已基本做完，剩下的是仿真器内的环境与训练闭环。**
> 未实现的部分：`pgmt/train/`（只有空包）与 `pgmt/envs/g1_env.py`（尚不存在）。
> `pgmt/envs/terrain/` 与 `pgmt/rewards/` 已实现并有单测，但**尚未接入任何仿真器**。

### M0 环境与骨架（本机 + 服务器冒烟）
- 交付：git init；仓库骨架；`setup/` 全部脚本；`smoke_test.py`（128 env 物理 + 10 iter PPO，验证 Isaac Gym PP4 × 5880 Ada × 驱动 580）；本机 conda 环境 + `tests/` 可跑通
- 验收：**服务器冒烟测试通过**（决策树主路线成功，否则切换 Isaac Lab 并更新本方案）；本机 `pytest` 全绿
- 状态：本机部分完成（82 个测试函数；本机 `pytest` 待重跑确认）；**待下载 PP4 tar + 服务器冒烟**
- **2026 复核修复**（原脚本有必然失败项，已改）：
  - `install_server_isaacgym.sh` 的 Python 版本探测 bug（`grep -oE "3[0-9]"` 从 `gym_39.so` 抠出 "39" → `python=3.39` 必然失败）→ 已修为取纯数字次版本号 + 兜底 3.9
  - 同一 2 位正则 bug 在 `check_isaacgym.sh`（仅打印）→ 已修；PTX 最大值比较改为数值排序（原字典序下 `compute_100 < compute_90`）
  - `smoke_test*.py` 仍按 Blackwell sm_120/cu128 判断，在 Ada 上会打误导性警告 → 已改为 sm_89/cu121 信息提示（不作为失败判据）
  - `smoke_test*.py` 的 main() 原会跑完全部阶段再把退出码归给首个失败项 → 已改为首个失败即停 + 后续阶段标 SKIP
  - editable 安装 isaacgym 会留 egg-link 指向 `mktemp -d` 临时目录 → 改为解压到持久路径 `~/isaacgym`
  - `smoke_test_isaaclab.py` 仅在成功路径 `close()`，失败会残留 Isaac Sim 占住 GPU → 改为 `finally` 中释放
  - `install_local.bat` 裸 `pip` 在 conda activate 失败时会污染 base 环境 → 增加 conda 存在性/激活生效/解释器路径三重校验，并改用 `python -m pip`

### M1 数据管线
- 交付：LAFAN1 下载（~144MB BVH，LFS 需 media URL）→ Mixamo→G1 重定向（含脚部接触标签：足速阈值法）→ 训练集/评估集切分（按论文按运动段跨序列采样）；参考帧预计算（含指数偏移采样、修正速度接口）
- 验收：重定向可视化检查（无穿地/关节跳变）；单测覆盖采样逻辑；运动段统计与论文描述一致
- 状态（2026-09-16）：已交付 `data/bvh.py`（解析器）、`data/retarget_lafan1.py`（重定向+批量导出）、`pgmt/envs/reference_sampler.py`（C^K 采样/A13 修正速度/A16 adaptive sampling）、`data/viz_retarget.py`（可视化）、`data/eval_retarget.py`（质量评估，报告在 data/processed/quality_report.csv）；74/74 测试全绿
- 重定向实现要点（数据驱动发现，与预想不同）：
  - LAFAN1 rig 约定：上=+x、前=+y、左=+z（left-handed 变体），旋转序 zyx，cm 单位；绑定姿态不可信（腿折叠）
  - 对齐为**常数** R_align = G1 链基静止朝向 × 循环置换 M_RIG（x→z,y→x,z→y），与运动数据无关；逐链欧拉分解 + 旋转均值居中（去绑定偏移）
  - 根朝向约定：rig → G1 基座（前+x/左+y/上+z）需右乘 Q_MRIG_INV=(0.5,0.5,0.5,0.5)；W 矩阵的 180° 旋转轴角提取有退化特例（对称矩阵），需 (W+I)/2 列提取
  - **评估口径（2026-09-16 校正）**：早期 22cm 的 FK 误差是在错误 root_rot 下测的（qW bug），正确基线 ≈26.5cm（走 22.2/舞 22.9/ground 62.6）；质量报告表与评级已按此口径
  - **实验结论（牛顿细化序贯分解，2026-09-16）**：倾斜链的精确逆（FK 组合互逆）在 FK 位置误差上反而变差（20.9→25.8cm）——精确末端朝向把中间连杆推离源姿态，而跟踪奖励正是连杆位置。已回退到朴素欧拉（文档在 _decompose_chain docstring）
  - **躯干专项（2026-09-16）**：18° 躯干误差的根源是 **FK bug**（torso_link 的关节映射缺失：waist_pitch_joint 定义在 torso_link 元素内，body[:-5] 映射失效 → 腰 pitch 从未应用）。修复后躯干朝向误差 **0.0°**；已加解析回归测试。同时腰链跳过旋转均值居中（绑定脊柱直立、均值=风格前倾 17°，居中会抹掉风格）
  - **万向节保护（2026-09-16）**：`_gimbal_protect()`——3 铰链链中间角近 ±90° 时外角提取退化，奇异区外角用邻域好帧 (sin,cos) 空间插值（80° 硬阈值；更低阈值/锥形过渡会误伤真实手势）。全量速度尖峰 0.31%→0.17%
  - **已知 v1 限制（收尾时点，2026-09-16）**：
    - 躺地/翻滚类（ground 5 序列）：FK 误差 63cm，非直立姿态分解退化——已建议排除出训练集（摔倒池用 fall/push 类 28cm）
    - 中等角度下腕/肘的表示分支翻转（快动作）：残余速度尖峰 ~0.17%，大半为源数据真实快手势（帧间 50-100°），非伪影；完整表示连续性改造列为 P2
    - 足底 −2~−5cm；接触一致率 59-79%
    - 质量报告：`data/eval_retarget.py` → `data/processed/quality_report.csv`（FK 误差总体 26.8cm：走 22.6/舞 23.3/ground 63.3）

### M1.5 IK 精修（2026-09-16 收尾）
- 交付：`data/ik_refine.py`（批量 LM 位置 IK，几何雅可比差分回归锁定三个实现坑：严格祖先 / 子 body 当前朝向轴 / 子 body 原点支点）；M1.5a 腿链（膝/踝 ≤8cm 验收通过）；M1.5b 全链 29 关节位置 IK；M1.5c 接触标签统一协议 + ground 类裁定（A17）
- 最终数字（`python -m data.eval_retarget`）：总体 FK **13.41cm**；非 ground 类均值 **13.47cm**；ground 类均值 **15.40cm**。质量报告同路径，可视化 `eval/viz/*.png`（walk/dance/jumps/ground 各一）
  > 数字口径（2026 复核）：以上由 `data/processed/quality_report.csv` 的 77 行实测值重新计算得出，取代早期记录的"总体 16.44cm / 非 ground 14.3cm"。**评级分布（A/B/C/D 计数）未落在 csv 中，需在可执行环境重跑 `python -m data.eval_retarget` 后回填**（依据 `eval_retarget.py:131-142` 的阈值手工核对时，多个序列的接触一致率/touchdown 恰在 A/B 边界上，不应手算）。
- **⚠️ 指标口径警告（2026 复核）**：`eval_retarget` 的 FK 误差与 `ik_refine.refine_full` 的优化目标**高度重叠**（`FULL_KEYPOINTS` 的 13 个映射在 `KEYPOINTS` 的 18 个里全部重复出现），因此该数值实质是**IK 的训练残差**而非独立度量；并且旧口径逐帧只用**骨盆平移**对齐、不能独立验证根朝向的物理正确性（骨盆平移对齐不会消除根旋转对其他点的影响，也不衡量累积平移漂移）。**引用该指标时必须同时报告这一点**，否则会高估参考质量。
  - **已改造（2026 复核）**：`data/eval_retarget.py` 现在把指标拆开 —— `fitted_err_cm`（13 个被优化点，**非独立**）、`holdout_err_cm`（4 个**未进入 IK 目标**的点：左右髋 `LeftUpLeg/RightUpLeg`、左右趾 `LeftToe/RightToe`）、`upright_holdout_err_cm`（同 4 点，但根朝向强制为单位朝向）、`root_rot_err_deg`（根朝向误差）。`tests/test_eval_metrics.py` 把"拟合组 == IK 目标"与"留出组 ∩ IK 目标 == ∅"锁成不变量 —— 未来若有人把被优化点误标为留出，测试会失败。
  - **独立性审计（2026 复核第二轮）**：那 4 个"留出"点虽没有目标牵引，但 `refine_full` 优化的是 `list(range(29))`——**全部 29 个关节**，改动髋/踝关节角会移动同一条运动链上的左右髋与左右趾，因此属**泛化检验**而非独立测量：值低不能证明物理可执行，值高需要检查目标映射、骨架差异及运动链。原先的 `abs_pos_err_cm` 因对**全部 17 个非基准点**取均值（含 13 个拟合点）而被移除并改名为只用留出点的 `upright_holdout_err_cm`；README 曾误标它为"独立"。
  - **`root_rot_err_deg` 是恒等式，不是指标（实测确认，已从结论依据中剔除）**：它在 IK 意义下确实独立（`root_rot` 由 `retarget` 冻结，`refine_full` 只读不写，已由 `tests/test_eval_retarget_integration.py` 与调用前快照逐位比较锁定），**但** `retarget()` 的 `root_rot = (qw ⊗ grot_src[Hips]) ⊗ Q_MRIG_INV` 与评估里 `source_root_quat_to_g1_base()` 是同一个式子，故与参考值按构造相等 —— 全量 77 个序列实测**恒为 0.01°**（float32 舍入），**无法区分任何序列**。一度把它写成"唯一完全独立的指标"是错的（独立 ≠ 有信息量）；换算正确性由 `tests/test_root_frame.py` 对真实数据的闭环测试保证。
  - 根朝向误差的乘法顺序已核对，并抽成唯一出处 `data/retarget_lafan1.py` 的 **`source_root_quat_to_g1_base()`**（+ `relative_rotation_angle_deg()`）：`root_rot = (qw ⊗ q_src) ⊗ Q_MRIG_INV`，qw 左乘（基变换）、Q_MRIG_INV 右乘（约定换算），两者不可交换不可省。改造中第一版把 qw 右乘得到错误朝向，已修正并配 `tests/test_root_frame.py` 锁死 —— 其中 `test_helper_matches_retarget_root_rot_on_real_data` 做闭环验证：把 helper 作用在源髋朝向上必须复原 `retarget()` 自己的 `root_rot`。
- **≤12cm 原验收线裁定为不可达（几何地板）**：G1 模型**无脊柱**——pelvis→torso_link 仅 4.4cm（腰三关节零长连杆），LAFAN1 Hips→Spine2 ≈31cm。Spine2（25cm）与双肩（22cm）误差是骨架拓扑不匹配的系统分量，任何关节角优化无法消除；踝/膝/手/前臂等可控关键点均 ≤12cm。新验收口径：非 ground 类 ≤15cm + 系统关键点（Spine2/肩）单独报告
- **链接方向残差实验否决（×2）**：两阶段（先位置后方向）与联合单阶段均使肘/手/膝变差（walk1 14.68→15.70cm）——源与 G1 肢体比例不同，方向目标与位置目标几何不一致。`refine_full` 最终 = 纯位置残差（负结果记录在 docstring）
- **接触标签统一协议（M1.5c）**：`contact_labels()` = 足速阈值（A14）+ 高度条件（地面=双踝 z 2% 分位 + 5cm 净空，排除空中缓速帧）+ 窗 5 中值滤波；源侧与 G1 侧共用，一致率在同等协议下比较（现 52.6–98.1%，此前循环度量已修复为 vs 源标签）
- **A17 ground 类裁定（2026 复核，全量 77 序列实测）**：A17 记录的理由是"ground1 躺地分解退化（38–116cm）"。重测后**该理由不成立，但浮现出一个更具体的缺陷，故暂不恢复**。

  **修复前 IK 的历史评估快照（n=77，不代表当前修复后的生成结果）**：

  | 类型 | n | 拟合cm | 留出cm | 直立cm | 尖峰% | 接触% |
  |---|---|---|---|---|---|---|
  | **ground** | 5 | **16.31** | **16.28** | **38.46** | 0.57 | 81.7 |
  | fall | 6 | 14.77 | 15.90 | 34.54 | 0.43 | 73.0 |
  | obstacle | 17 | 14.36 | 15.37 | 27.46 | 0.20 | 88.7 |
  | walk | 12 | 13.79 | 14.81 | 26.02 | 0.08 | 86.4 |
  | *总体* | *77* | *14.25* | *15.22* | *28.58* | — | — |

  1. **记录的理由不成立**：ground 的 `holdout_err_cm` = 15.26 / 15.80 / 16.12 / 17.04 / 17.18（均值 16.28），与 **fall（15.50–16.47）完全重叠**，距总体均值仅 +7%；`spike_pct` 0.57%、`contact_agree` 81.7% 均正常。原记录的 38–116cm 在当前数据上无从复现（来自 `lafan1_g1_v1_snapshot` 时期）。
  2. **但 ground 有一个真实的特异异常，且不是它记录的那个**：`min_foot_z` = **0.493 / 0.418**（ground1_subject4 / subject1）是全表最大，为第三名（0.189）的 **2.2 倍** —— 这两个序列的踝原点最低高度约 40–50cm；躺地时身体其他部位可能接地，不能据此认定全身悬空；其 `upright_holdout_err_cm` = 49.64 / 39.40 也是全表最高两位。这与旧观测 `eval/viz/ground1.png`（源数据平躺、G1 站立）很可能是同一根因。
  3. **同类缺陷也出现在保留集里，且更普遍**：**14/17 个 `obstacle` 序列存在踝原点低于名义平面的一帧或多帧**，最深 `obstacles5_subject2` = **−0.286 m**（G1 静止踝高仅 0.036 m，提示需要结合逐帧足底碰撞几何与目标地形检查，不能仅由 body 原点给出整段穿地结论）。
  4. **统一怀疑**：`retarget()` 的垂直锚定（`data/retarget_lafan1.py:414-422`）以「全序列最小的（足高 − 髋高）」作为"地面接触点"。当动作里**脚不代表最低接触点**时该假设失效 —— 躺地（脚抬在空中）与跨越障碍（髋压得极低）恰是两类反例。这能同时解释 2 与 3。

  **行动项（按序）**：
  - 先跑 `python -m data.probe_heights`（默认查 ground1_subject1 / ground1_subject4 / obstacles5_subject2 / walk1_subject1 作对照），确认锚定假设是否失效。**它是纯诊断，不替你做裁定。**
  - 若确认锚定失效 → 修锚定（需给出一个不依赖"脚触地"的垂直对齐判据），再全量重跑，然后才谈是否恢复 ground。
  - 在完成碰撞几何、接触和高度的独立复核前**保留 A17 的过滤行为**；踝原点高度与单位根朝向对照不足以单独裁定恢复或排除。

  <details><summary>重测命令与保护</summary>

  ```
  python -m data.eval_retarget --only ground   REM 复核用；写 quality_report_ground.csv
  python -m data.eval_retarget                 REM 全量；才写 quality_report.csv
  python -m data.probe_heights                 REM 高度剖面诊断（查锚定）
  ```
  ⚠️ `--only` **绝不**写完整报告（`resolve_out_path` 保护）。改造前 `--only ground`
  会把仓库里 77 行的报告静默覆盖成 5 行 —— README 恰好推荐用这条命令做复核。

  </details>
- 其余：G1 29 个驱动关节均有机械限位，qpos 保持在各自限位内、qvel 直接按这些有界坐标差分（不使用 wrap/unwrap）；本机 pytest 需 `tests/conftest.py` 设 KMP_DUPLICATE_LIB_OK（Anaconda MKL × torch 双 OpenMP 运行时导致 SVD Abort）；当前 **730 个测试（本机 728 通过、2 CUDA 跳过）**，其中约 55 个为 data-gated（需 `data/raw/lafan1/*.bvh` 与 `data/processed/lafan1_g1/*.npz` 才执行，缺失时自动 skip）

### M2 Stage 1：Tracking Pretraining
- 交付：g1_env（观测 96+10×96+C^K、PD 控制）、History Encoder、IFM(RoPE)、Actor、三头 critic、Table I 上半部奖励（root-centric）、recovery curriculum、adaptive sampling、PPO 训练入口
- 验收（服务器平地训练）：多种运动（走/跑/舞/蹲/大幅动态动作）可跟踪；平地 episode 完成率 > 90%；body pos RMSE < 0.1m 量级；与 OmniH2O/HOVER 平地水平相当；**保存 stage1 checkpoint（供全部 Stage 2 变体复用）**

#### M2.0 观测契约（2026 复核新增，已完成 —— 纯逻辑，本机可测）
M2 之前先把观测规格从"散落在多个模块的局部常量 + docstring"固化成可校验代码，
避免环境层（`g1_env.py`）边写边猜。交付：

| 文件 | 内容 |
|---|---|
| `pgmt/contracts.py` | 跨层共享维度常量：`OBS_DIM=96` / `ACT_DIM=29` / `HISTORY_LEN=10` / `REF_FRAME_DIM=61`（此前 96 在 policy 层硬编码了两份） |
| `pgmt/policy/rotation.py` | 旋转矩阵↔**6D 表示**（`rotmat_to_6d`/`rot6d_to_rotmat`，Zhou et al. 2019）、**四元数最短弧插值**（`quat_slerp`，含半球对齐 + NLERP 稳定分支）、`relative_anchor_6d`（`e_t` 的计算） |
| `pgmt/envs/observations.py` | `OBS_LAYOUT`（96 维逐段 offset/dim）、`build_obs`/`build_obs_from_state`、`HistoryBuffer`（`H_t`，含 `observe_then_advance()` 固定取值/推进顺序）、`PrivilegedLayout`（A9 维度表） |
| `pgmt/envs/reference_sampler.py` | 新增 `ref_rot()`（参考锚点朝向的小数帧插值）+ `MotionDatabase.from_sequences()`（内存构造，供单测）；`AdaptiveSampler` 热路径优化（候选段与权重缓存 + 向量化权重，`update()` 时失效），并把 `push` 语义写进契约 |
| `data/retarget_lafan1.py` | 新增 `source_root_quat_to_g1_base()` / `relative_rotation_angle_deg()`（根朝向比较的唯一换算出处，供 `eval_retarget` 复用） |
| `tests/test_rotation.py`、`tests/test_observations.py`、`tests/test_contracts.py`、`tests/test_root_frame.py`、`tests/test_reference_integration.py`、`tests/test_rewards_spec.py` | 新增单测；`tests/test_reference.py`、`tests/test_eval_metrics.py`、`tests/test_assumptions.py` 扩充 |

**无仿真器集成冒烟**（`tests/test_reference_integration.py`，需真实数据）：用真实重定向序列跑通"运动库 → C^K → H_t → o_t → HistoryEncoder/IFM/Glimpse/Actor/四头 critic"整条链路（含梯度）。其中 `test_ref_rot_trajectory_is_continuous_over_real_data` 按控制频率扫真实轨迹，断言相邻步的参考朝向变化 < 30° —— 这是最短弧插值的验收（缺半球对齐会在 `q`/`−q` 翻转点产生接近 180° 的假跳变）。这样在搭 `g1_env.py` 之前就能发现接口错配，不必等 Isaac Gym 就位。

**别名安全（环境层的真实用法）**：`HistoryBuffer.push()` 会**复制**输入。环境层典型写法是复用 `obs_buf` 并 `hb.push(obs_buf[i])` —— `np.asarray` 对已是 float32 的输入是零拷贝，若不复制，缓冲里存的就是同一底层内存的视图，下一次写入会篡改"历史"，症状是策略输入悄悄退化为当前帧的重复。已加测试锁定（`test_history_push_copies_input_so_later_mutation_is_safe` 等）。

**取值顺序**：环境层每步应调用 `HistoryBuffer.observe_then_advance(o_t)`（先取 `H_t` 再推入本帧）。顺序搞反（先 push 再取）会让 `H_t` 含入当前帧，**不会报任何错**，只是策略输入多了一帧自身观测 —— 故把顺序封装进方法而不是靠调用方自觉，并有 `test_observe_then_advance_excludes_current_frame` 锁定。

**归一化防护**：`relative_anchor_6d`（`e_t` 的计算）会先归一化输入。非单位四元数不会报错，但结果**会被污染**。

> ⚠️ 此处我连续写错过两次理由，已在 pytest 实测后更正（`tests/test_rotation.py::test_quat_to_mat_is_not_scale_equivariant` 把正确事实固化）：
>
> `quat_to_mat` 对非单位四元数**既不是**整体放大 ‖q‖²，**也不能**靠"Gram-Schmidt 把缩放消掉"来免责。实现里的公式是 `a[i,i] = 1 − 2(另两个分量平方和)`，齐次项 `1` **不随尺度变化**。取 `q = [0,3,0,0]`：`a[0,0] = 1 − 2(0+0) = 1`（不受 x 的尺度影响），而 `a[1,1] = a[2,2] = 1 − 18 = −17`。即缩放**只体现在部分对角元上**，矩阵与 s²·R 之间差一个不随 s 变的偏置。
>
> 后果：当两个输入尺度不同（如真机状态估计 vs 归一化参考）时，相对旋转的两个修正项尺度失配，`e_t` 被算错。归一化因此是必要的，`test_relative_anchor_6d_mismatched_scales_would_differ_without_norm` 是该防护的反例守卫。
>
> 真实来源：slerp 浮点残差、真机状态估计、手写常量四元数。

**采样热路径**：`AdaptiveSampler._candidates()`/`_weights()` 在 15k 并行环境下每次 reset 都会调用；原实现每次都重建候选段列表并逐项查字典。现改为缓存 + 向量化（`test_weights_match_reference_formula` 逐项对齐原公式，`test_candidates_match_reference_enumeration` 对齐原枚举）。另外 `seg_len<=0` 现在构造期就报错。

#### M2.0b 奖励规格（2026 复核新增，已完成 —— 纯逻辑，本机可测）

`pgmt/rewards/spec.py`：把论文 Table I + Eq.10 的**代数结构**固化为可单测代码，
各项残差的计算留给环境层（`g1_env.py`）。

| 内容 | 说明 |
|---|---|
| 逐项权重 | Table I 四组（upper 6 / lower 6 / aux 10 / terrain 6）照搬；`tests/test_rewards_spec.py` 用**论文表格的字面量副本**逐项断言，抄错即失败 |
| 分组求和 | `total_reward(stage2=...)` 实现 Eq.5；**未知项名直接报错**（避免环境层写错键名后静默少算一项奖励，症状只是"训练不动"） |
| 松弛算子 | Eq.10 `ẽ = [e − α·χ(κ)·τ]₊`，含 χ 只在 slopes/stairs/boxes 激活、α=0/χ=0 退化为严格跟踪、τ 随难度线性增长并饱和 |
| α 解析 | `resolve_alpha()`：非 TA 项**恒返回 0** —— 保证 upper body 与其余约束默认严格（论文要求），避免调用方自己判断时静默放宽 |

**术语澄清**：论文 `τ` 一符两用 —— Eq.1 的 τ 是未来参考帧的**时间偏移**，
Eq.10 的 τ_{mh} 是误差空间的**容忍预算**（量纲同误差）。代码一律用 `tau` 指后者。

**核函数（2026 更正）**：论文只说 ẽ "replaces `e` in **the** exponential tracking
reward" —— 定冠词表明是引用领域既有约定而非自定义。核取 **`exp(−e²/σ)`（高斯式，
误差的平方）**，依据是 PGMT 明示继承的 tracking 实现：OmniH2O 论文奖励表把 body
position 写作 `exp(−0.5‖p−p̂‖²₂)`，其配置注释亦明写
`tracking reward : exp(-error^2/sigma)`。

> ⚠️ 我最初按"通用形式"记成了线性核 `exp(−e/scale)`，**是错的**，已更正。
> 两者不可通过重参数化互换（线性核下 `ln r ∝ e`，高斯核下 `ln r ∝ e²`），
> `tests/test_rewards_spec.py::test_kernel_is_squared_error_not_linear` 锁定这一点。

**σ 取值**：论文未给 → `pgmt/rewards/spec.py` 的 `SIGMAS` 表，量级与"上/下半身
分离"的结构参照 OmniH2O。σ 的量纲是**误差平方**（位置 m²、关节 rad²），不可与
线性核的"误差尺度"直接换算；OmniH2O 位置项的 0.5 系数已吸收进 σ（`σ_us = 2·σ_oh`）。
上半身位置 σ 明显小于下半身，对应论文 §IV"upper-body 保姿态保真 / lower-body
让位给平衡与接触"的设计意图，有测试锁定该相对关系。

**σ 的可验证依据**：σ 不是可随便拍的调参量 —— 取小了该项在任何误差下奖励≈0
（项是死的、无梯度），取大了所有误差都≈1（无区分度、等于该项消失）。两条退化
都由 σ 与真实误差分布的关系决定，因此可测量：

```
python -m data.probe_reward_scales            # 量真实参考运动量级 + 各 σ 的响应区
python -m data.probe_reward_scales --json out.json
```

> ⚠️ **该探针能做什么、不能做什么（勿当成已验证）**：
> - **能**：用真实数据暴露**量纲错误**；给出各目标的误差物理上界；
>   给出每个 σ 的"可响应区"（reward ∈ [0.1,0.9] 对应的误差区间，解析解）。
> - **不能**：验证 σ 已标定正确。探针量到的是**参考运动本身的幅度**
>   （如 `joint_range_rad p95 = 5.54 rad`），**不是跟踪误差**。参考每帧动
>   0.6 rad 只说明动作快，好的跟踪误差可能只有 0.05 rad。
> - 因此**σ 的最终标定必须等训练时读到实际跟踪误差分布**，届时应重跑本探针
>   并把实测误差分布叠加对照，再决定是否调整 σ。
>
> `tests/test_reward_probe.py` 把探针的解析解与核函数交叉验证，防止用一条
> 算错的曲线去论证 σ。

**Table I 逐项语义对照**（`pgmt/rewards/semantics.py`）：把 28 条（含上下半身
同名项）逐项标注与参照实现的对应关系 —— `IDENTICAL` **11** 条 / `APPROX` **7** 条 /
`PGMT_SPECIFIC` **6** 条 / `UNRESOLVED` **4** 条。

**论文对照待核对四项**（`UNRESOLVED`；现已按 A18/A21 的显式假设实现，以下记录未公开的定义边界）：

| 项 | 待定内容 |
|---|---|
| `aux.head_torso_impact` | 论文只给项名与权重（−1e−5），未说"撞击"如何度量（接触力？冲量？穿透？） |
| `aux.ee_accel_mismatch` | "mismatch" 是与**参考**比还是与**上一帧**比，论文未说 |
| `aux.floating_anchor_pos` | 语义依据来自 HOVER/PHC 的 floating-base 表述，但未在参照配置中确认到同名项 |
| `lower.ta_link_ori` | 参照实现有 body_rotation（σ=1.0），但未验证它是否按上下半身分离，故无法判断 TA 朝向是"新增"还是"分离既有项" |

> **标注纪律由测试强制**（`tests/test_reward_semantics.py`）：标 `IDENTICAL`/
> `APPROX`/`PGMT_SPECIFIC` 都蕴含"我知道参照实现那边是什么样"（存在性断言），
> 因此 note 里出现"未确认/未见/未验证/依据不足"等保留措辞时**只能标
> `UNRESOLVED`** —— 这条规则本身是被第一次跑测试暴露出来的：我原先把
> `ta_link_ori`、`floating_anchor_pos`、`ee_accel_mismatch` 三项按"没见过"当成
> "不存在"标成了已确认，被 `test_hedged_notes_must_be_unresolved` 抓住。

> 注意：该表**不验证权重数值**。各实现的权重表不可直接比对 —— legged_gym 系
> 常按 `dt` 归一化、跟踪 body 数量也不同，量级自然不同（实测 `dof_pos_limits`
> −125 vs 论文 −15.0；`stumble` −1250 vs −20.0）。数值一律以论文 Table I 为准。

**TA 的含义**：Table I 中带 TA 的三项即 Eq.10 松弛作用的 lower-body 目标
（link position / link orientation / joint position）；lower body 的速度项
**不带** TA，即速度跟踪仍严格。

**新增假设 A18**（`RewardImpl`）：核函数形式、σ 来源、τ 的斜率/饱和值 —— 集中在
假设表并由测试校验（τ 饱和值须与 A12 的"5% 量级"一致；σ 来源须可追溯）。

**维度闭环**：`tests/test_contracts.py` 用"默认构造策略模块 + 按契约形状喂数据"锁住 policy 层与 `pgmt.contracts` 的一致性（HistoryEncoder 接受 96、Actor 输出 29、IFM 接受 61×K、critic 头数 3/4/1），避免哪一层又退回硬编码。

**已固化的语义决定**（论文未明确，属实现选择，需在最终报告说明）：
- **`H_t` 不含当前帧 `o_t`**（`H_t = [o_{t−10}, ..., o_{t−1}]`，最近一帧在末行）。理由：`o_t` 已单独作为 History Encoder 的 query 与 Actor 输入，含入只是重复信息。由 `HistoryBuffer` 强制 + 边界测试锁定。
- **冷启动填充**：前 10 步用 `fill` 在**前端**填充（沿用 HOVER/OmniH2O 零填充惯例），使观测维度恒定而非可变长度。已支持分段填充 `fill=e_t_identity_fill()`：`e_t` 段填 6D 恒等旋转、其余段填 0 —— 因为 **6D 零向量不是合法旋转编码**（`rot6d_to_rotmat([0]*6)` 退化），零填充等于喂 10 帧分布外输入。默认仍为零填充（工程惯例，只有 episode 前 10 步受影响），**实际取值需在报告中说明**。
- **`o_t` 不含基座线速度**：线速度属特权观测（A9）只给 critic；参考平面速度经 `C^K` 的 `ṽ^r` 进入策略（论文 Eq.1）。
- **`e_t` 的定义**：`R_rel = R_ref^T · R_robot`（参考锚点系下的机器人朝向），取 6D 表示。对全局旋转不变，与"root-centric、减少绝对朝向依赖"一致；有单测锁定该不变性。
- **`H_t` 的帧序**：时间正序（末行最新）。内部按正序存储，避免与 `appendleft` 式实现产生方向歧义。
- **A9 特权观测维度表**（默认 `n_envs_dynamics=1` 共 48 维）：`base_lin_vel`(3)、`base_ang_vel`(3)、`foot_contact_states`(2)、`friction_coefficients`(2×N)、`terrain_height_at_feet`(2)、`base_mass_perturbation`(1×N)、`com_perturbation`(3×N)、`push_perturbation`(3)、`motor_strength_scale`(29×N)。此前测试里的 `PRIV_DIM = 20` 是占位值，已废弃。

### M3 地形系统（纯逻辑部分 ✅ 完成）
- 交付：5 族 × L0–L9 生成器、高程图渲染（含噪声/扰动）、level-based 课程、兼容规则、地形相关随机化与延迟扰动
- 验收：高程图单测（解析高度与采样一致）；可视化各难度地形合理（对齐 A7）
- **状态**：纯逻辑部分已完成并通过测试；**地形 mesh 注入 Isaac Gym 属 M2**。

| 交付项 | 实现 | 关键设计 |
|---|---|---|
| 5 族 × L0–L9 解析高度场 | `pgmt/envs/terrain/generators.py` | A7 端点线性插值；四周留白使跨 tile 高度连续；**tile 边长 > 2m**（否则一张高程图跨多 tile，"族/难度"无定义）→ A19 |
| 21×21 yaw 对齐高程图 + A8 扰动 | `pgmt/envs/terrain/elevation_map.py` | 与 `glimpse_encoder` 的 `(center+1)/2·(n−1)` 约定逐位一致；**A8 的 map_res 与 map_size 不是独立参数**（res 必须 = 2/(n−1)）；存**相对基座**高度 |
| level-based 课程（升降级） | `pgmt/envs/terrain/curriculum.py` | 每环境独立状态机；只调 `level` 不调 `family`（族是评估协议里 matched 的一部分） |
| behavior–terrain 兼容规则 | 同上 | 规则表取自 A11（排除表，故对未分类运动**保守放行**）；运动类别由 LAFAN1 序列名前缀解析（12 个前缀） |

**验收证据**：
- 解析对照：`slopes` 的 `rise = inner·tan(θ)`、`stairs` 的 `clip(idx−1,0,级数−1)·阶高`、`boxes` 只出现 0/方块高两个取值 —— 均有测试
- **闭环**：生成的高程图喂给真实的 `TerrainGlimpseEncoder`，采样值与解析值逐点一致；另有**转置检查**（对称测试抓不到的那类错位）
- 课程**转移表**穷举（连续成功升级、失败清零计数、L0/上限夹紧、多环境互不影响）
- 兼容规则**保守性**：未分类运动在所有族/难度上均放行

**未做（归入 M2 清单）**：论文说 level "jointly controls ... **selected termination
delays**" —— 终止条件本身尚未定义（见下方 M2 待办），故延迟项无法单独落地。

**可视化**：`python -m data.viz_terrain`（五族 × 十级总览）、
`python -m data.viz_terrain --maps-family stairs --levels 0 5 9`（高程图）、
`python -m data.viz_terrain --dump stairs 9`（无 matplotlib 时打印数值）。

#### M2 待办清单（写 `g1_env.py` 之前必须定掉）

按"是否阻塞环境层"排序。前四项**纯逻辑、本机可做**，最后一项需服务器。

| # | 待办 | 依据/难点 | 阻塞谁 |
|---|---|---|---|
| 1 | ✅ **Table I 各残差的实现**（tracking / auxiliary / terrain-contact） | 论文 Table I 只给权重与项名，残差的**具体形式**（在哪个坐标系量、对哪些 body/joint、如何归约）未写 —— 已实现于 `pgmt/rewards/{tracking,auxiliary,terrain_contact}.py`，28 项语义对照见 `semantics.py`，度量与尺度见 A21/A22 | —（已解除） |
| 2 | ✅ **终止条件** | 论文只说 "reaches the maximum rollout horizon without early termination"，未列触发条件 —— 已实现于 `pgmt/envs/termination.py`（三类触发 + 容忍区复用 A12 松弛预算，A20） | —（已解除） |
| 3 | **摔倒状态池（A10）** | 论文说 "offline pool of post-fall robot states"，未说状态如何表示、如何重放 | recovery curriculum |
| 4 | **多头 critic 的 PPO** | `pgmt/policy/multi_head_critic.py` 已有网络（含 Eq.9 的 3 头→4 头扩展）；缺的是 PPO 算法侧 —— rsl-rl 的 `ActorCritic` 是单价值头，需自定义 policy/algorithm 类，Fig.4 的 split vs aggregate 消融落在这里 | Stage 1 训练闭环 |
| 5 | 地形 mesh 注入 Isaac Gym（heightfield/trimesh） | 需仿真器；`generators.py` 已提供解析真值 | M2 环境 |

> **#1 与 #2 完成时的两条教训**（都属"静默错误"，已在代码里加了防线）：
> 一是惩罚项返回负值时，与 Table I 的负权重相乘会变成**正贡献**（惩罚反转成奖励），
> 现在 `spec.RewardGroup.sum` 在运行时拒绝负值/NaN；二是"值得奖励的量"与"值得惩罚的量"
> 必须有统一值域（全部 ≥ 0），符号只在权重里 —— 这条已写成不变量并被测试锁定。

**已登记但可推迟的待办**（论文依据不足，见 `pgmt/rewards/semantics.py` 的
`UNRESOLVED` 四项）：`head_torso_impact`（撞击如何度量）、`ee_accel_mismatch`
（与参考比还是与上一帧比）、`floating_anchor_pos`、`ta_link_ori`（参照实现的
body_rotation 是否已按上下半身分离）。这些项已有实现假设；其中三个 auxiliary 项属于 Stage 1 也使用的奖励组，不能因为参照关系未确认而从 Stage 1 忽略。UNRESOLVED 表示论文对照依据未闭合，并非代码未实现。

### M4 Stage 2：Perception Injection
- 交付：Glimpse Encoder（位置选择器+双线性采样）、IFM 注入、四头 critic、Table I 下半部接触奖励、tracking relaxation、global position correction、训练入口
- 验收：全基准 completion 显著高于 PGMT-Pretrain（论文 40.02%→87.81%，目标 ≥80%）；L9 ≥ 70%；stairs 上相对消融的优势趋势出现

### M5 基准评估与消融
- 交付：benchmark_dataset（9600 matched episodes，种子固定）、metrics（含 Wilson 95% CI）、4 个感知消融 + Aggregate-critic 变体训练与评估
- 验收：产出对标 Table II / Fig. 3 / Fig. 4 的完整表格与图；数值与论文排序一致（Ours > CNN > 2Glimpse/Fixed > NoHeight，stairs 差距最大）；绝对值偏差在 ±5~10% 内（超出则在报告假设章节解释）

### M6 基线与最终报告
- 交付：RGMT-Reimpl（读 RGMT 论文实现，尽力而为）；SONIC external checkpoint（查询 NVIDIA 是否可获取，拿不到则报告空缺并引用论文数字）；最终复现报告（对标表 + 训练成本实测 + 假设与偏差说明）
- 验收：报告覆盖论文全部实验条目，未完成项有明确说明

## 6. 训练运行矩阵（服务器 10× RTX 5880 Ada，sm_89）

| 运行 | 目的 | 依赖 | 优先级 |
|---|---|---|---|
| Stage 1 | 全部后续的基础 | - | P0 |
| PGMT-Ours (Stage 2) | 主结果 | Stage 1 | P0 |
| PGMT-Pretrain 评估 | 主表基线 | Stage 1 | P0 |
| 4 个消融 + Aggregate-critic | Table II/Fig. 3/4 | Stage 1 | P1 |
| RGMT-Reimpl | 基线 | 无 | P2（尽力） |
| SONIC external | 基线 | 外部 ckpt | P2（尽力） |

- 论文实测参考：~8.4 s/iter，29 GiB/卡，15k envs/卡（论文用 4× RTX 5090）。若单卡显存不足可降 envs 数并在报告记录
- **本机 48GB/卡与论文 5090（32GB）不同**：VRAM 余量更大，可先按 15k envs 试跑，实测后回填本机 Time/VRAM 列（Table II 的成本列按方案约定用我们自己的实测值）
- 消融与 Ours 共享 Stage 1 checkpoint，节省计算

## 7. 风险清单

| 风险 | 影响 | 对策 |
|---|---|---|
| PP4 在 5880 Ada 上不可用 | 阻塞一切训练 | M0 前置冒烟（`check_isaacgym.sh` + `smoke_test.py`）；兜底 Isaac Lab（`install_server.sh`） |
| 驱动 580 超出 PP4 社区验证范围 | create_sim/PhysX 初始化崩溃 | 冒烟一跑便知；失败则降驱动到 535/545 档，或转 Isaac Lab |
| 论文超参未公开 | 指标偏差 | A1–A22 假设表集中管理 + 报告说明 |
| 参考质量指标不独立（自证） | 高估参考质量、错误的 A17 裁定 | 改用留出关键点 + 绝对位置/根朝向误差；见 M1.5 的指标口径警告 |
| LAFAN1 获取或许可 | 数据阻塞 | 研究用途免费；备选 AMASS 子集（需重新对齐重定向管线） |
| 重定向质量不达标 | tracking 上限受损 | 参考 OmniH2O/PHC 成熟管线；必要时联系作者 |
| RGMT-Reimpl 工作量 | 基线缺失 | 排 P2；先完成主结果 |
| 本机无法验证训练代码 | 迁移服务器后集中爆错 | 纯逻辑全面单测 + 服务器上先跑 `pytest tests`（requirements.txt 已含 pytest） |
| 训练时长 | 多 run 排队 | 优先级排序；checkpoint 复用；先跑 Ours+Pretrain 再跑消融 |

## 8. 不做的事（范围外）
- 真机 G1 部署、LiDAR 建图、Jetson 推理
- teleoperation（PICO/GMR）与 motion matching 命令源
- 真机扰动/摔倒恢复实验
