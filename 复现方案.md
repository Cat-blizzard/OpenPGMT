# PGMT 复现方案（Reproduction Plan）

> 论文：PGMT: Perceptive General Motion Tracking for Humanoid Robots（arXiv:2609.08511v2，2026-09-09）
> 项目主页：https://luyili.github.io/pgmt/ （截至方案制定时**官方代码未开源**，需自行实现）

## 0. 目标与范围

| 决策项 | 结论 |
|---|---|
| 复现保真度 | **完整对标**：Table II 主表 + Fig. 3 消融 + Fig. 4 critic 对比 |
| 范围 | **纯仿真**（Isaac Gym），不做真机部署、teleoperation、motion matching、LiDAR 建图 |
| 开发环境 | 本机 Windows：写代码 + 纯逻辑单元测试 + 环境脚本；**无法本地跑 Isaac Gym** |
| 训练环境 | 用户服务器：4× RTX 5090（Linux），代码写好迁移过去训练 |
| 基线 | 必做：PGMT-Pretrain、4 个感知消融（NoHeight/CNN/2Glimpse/Fixed）、Aggregate-critic；尽力而为：RGMT-Reimpl、SONIC v1.1 External |

## 1. 论文实现规格摘要

### 1.1 机器人与动作
- Unitree G1，29-DoF；动作 `a_t ∈ R^29` = 关节位置目标，经 PD 控制器转力矩
- 控制频率假设 **50 Hz**（论文未明说，按 G1 惯例，记为假设 A1）

### 1.2 观测空间
- `o_t ∈ R^96` = [参考相对 anchor 朝向 `e_t`(6, 6D 旋转), 基座角速度 `ω_t`(3), 关节位置 `q_t`(29), 关节速度 `q̇_t`(29), 上一动作 `a_{t-1}`(29)]
- `H_t ∈ R^{10×96}`：10 帧 proprioception 历史
- `C_t^K`：未来参考帧，时间偏移 `τ = t + 2^k − 1`（k=0..K−1），每帧 61 维 = [`q^r`(29), `q̇^r`(29), `v^r`(3, 修正后平面 anchor 速度)]
- `M_t ∈ R^{21×21}`：yaw 对齐 2m×2m 高程图（仅 Stage 2 引入）

### 1.3 策略架构
```
Stage 1（平地预训练）                    Stage 2（感知注入）
o_t ──Q──┐                              o_t ──Q──┐
H_t ──KV─┴→ History Encoder(MHCA) → s_hist       │
              │                                   │
C^K ──KV──────┴→ IFM(MHCA+RoPE) → s_int ←──┴──── 地形 token z_1..z_4
                     │
        ┌────────────┴────────────┐
   Actor: MLP([o_t, s_int]) → a_t    Critic: MLP(o_priv, s_int) → V_upper/V_lower/V_aux(/V_terrain)
```
- **Terrain-Glimpse Encoder**（Stage 2 新增）：
  1. 位置选择器 `MLP_φ(vec(M_t), s_hist, C^K)` → 4 个 glimpse 位置 `r_j ∈ R^2`
  2. 双线性采样裁剪 5×5 patch（可微，无需落足点标注）
  3. `MLP_ψ(patch, ρ_j)` → 地形 token `z_j`
  4. 注入 IFM：`K = V = [s_hist; C^K; z_1..z_4]`
- **Multi-head critic**：共享 backbone，Stage 1 三头（upper/lower/aux），Stage 2 改末层为四头（+terrain）

### 1.4 奖励（Table I，照搬）
| 组 | 项（权重） |
|---|---|
| Upper body | link pos(1.0), link ori(1.0), link lin vel(0.5), link ang vel(0.5), joint pos(1.0), joint vel(0.5) |
| Lower body | TA link pos(0.5), TA link ori(2.0), link lin vel(0.5), link ang vel(0.5), TA joint pos(0.5), joint vel(0.5) |
| Auxiliary | root ori(0.5), corrected root vel(2.0), floating-anchor pos(1.0), recovery upward vel(12.5), pelvis vert accel(−1e-3), EE accel mismatch(−1e-3), action rate(−0.05), joint limit(−15.0), undesired contact(−0.1), head/torso impact(−1e-5) |
| Terrain-contact（仅 Stage 2） | touchdown quality(10.0), reference contact match(1.5), slip(−1.0), stumble(−20.0), contact switching(−30.0), contact force(−1e-6) |

### 1.5 两阶段训练机制
- **Stage 1**：平地；root-centric tracking（误差相对参考根 anchor 计算，不依赖世界系绝对位姿）；recovery curriculum（离线摔倒状态池，随 episode 生存率渐进提高池初始化比例）；adaptive sampling（跟踪失败频繁的运动段提高采样概率，保持全覆盖）
- **Stage 2**：加载 Stage 1 权重，注入感知。训练级调整：
  - **Terrain-aware tracking relaxation**：`ẽ = [e − α·χ(κ)·τ(d)]₊`，仅作用于 lower-body link pos / link ori / joint pos 目标；`χ(κ)` 仅在 slope/stairs/boxes 激活；`τ(d)` 随难度线性增长并饱和；`α_{h,j}` 逐元素缩放
  - **Global position correction**：`v^r = v^r + clip(g(‖v^r‖)·λ_pos·e^p)`，平面位置误差经参考系旋转后反馈到速度指令，静止时门控 `g(·)` 归零
  - **Terrain curriculum**：flat/slopes/stairs/boxes/random rough 五族 × L0–L9，level-based 升降级；behavior–terrain 兼容规则排除明显不可行组合

### 1.6 评估协议
- **9600 个 matched 30-s episodes** = 5 地形族 × 10 难度 × 192 集；各策略使用完全相同的运动参考、起始帧、地形分配、动力学随机化、观测扰动、执行器延迟
- 指标：Completion %、L9 %、body pos RMSE(m)、body ori RMSE(°)、joint RMSE(rad)、contact F1 + Wilson 95% 置信区间（Fig. 3）
- 训练成本列（Time/iter、VRAM、Envs/GPU）用我们自己的实测值

## 2. 关键假设清单（论文未公开的细节，需拍定并记录）

| # | 假设 | 默认取值 | 备注 |
|---|---|---|---|
| A1 | 控制频率 | 50 Hz（dt=0.02s） | G1 惯例；30s=1500 步 |
| A2 | 未来参考帧数 K | 6（偏移 0,1,3,7,15,31） | 论文只给公式未给 K |
| A3 | token 维度 / MHCA 头数 | 256 / 4 | 按 OmniH2O/HOVER 规模 |
| A4 | Actor/Critic MLP | [512,256,128] | 同上 |
| A5 | RoPE 维度 | 64（每头 16） | 常规设置 |
| A6 | PPO 超参 | clip 0.2, γ=0.99, λ=0.95, 学习率 1e-3 递减, 24-step horizon | 按 rsl-rl 惯例 |
| A7 | 地形难度参数（每族 L0→L9） | slope 5°→30°；stairs 阶高 4→24cm；boxes 高 5→40cm；rough 幅值 1→12cm；flat 难度=随机化强度递增 | 论文未给数值；40cm 对齐真机"37cm"上限 |
| A8 | 高程图噪声模型 | 高斯噪声(σ 随难度) + 随机 dropout 网格 | 论文提到 observation corruption 未给细节 |
| A9 | privileged 观测集 | 基座线速度、足端接触状态、摩擦、地形高度@足端、质量扰动等（Isaac Gym 标准集） | 论文未列 |
| A10 | 摔倒状态池采集方式 | 训练时在 termination（摔倒）时刻记录 `[o_t, 参考上下文]` 存入环形池 | 沿 RGMT 思路 |
| A11 | 兼容规则实现 | 运动特征分类器（躺/趴/倒立类不采样于 stairs/boxes 高级别；坐姿类不采样于斜坡高级别） | 论文只说是"粗规则" |
| A12 | 松弛参数 | `α_{h,j}=1`（lower-body 元素）、`χ` 按地形族、`τ(d)` 为难度线性+饱和函数（饱和值=各目标 5% 量级） | 论文只给形式 |
| A13 | 速度门控与修正增益 | `g(‖v‖)=smoothstep(0, 0.5, ‖v‖)`，`λ_pos≈1.0`，clip `v̄=1.0 m/s` | 论文只给形式 |
| A14 | 重定向管线 | Mixamo(LAFAN1) → G1：骨骼缩放 + 逐关节旋转映射，参考速度由有限差分 | 沿用 OmniH2O 思路 |
| A15 | Glimpse 编码器结构 | Ng=4、patch 5×5（论文给定）；MLP_φ 隐层 (512,256)、MLP_ψ 隐层 (256,)、位置输出 tanh×1.0m | 论文只给数量与 patch 尺寸；MLP 规模按 A4 风格 |
| A16 | Adaptive sampling | 段权重 = 1 + β·失败次数，β=1.0（软加权，权重下限 1 保留全覆盖） | 论文只说失败频次提高概率并保留全覆盖 |

> 所有假设在代码中集中于 `pgmt/cfg/assumptions.py`，训练日志记录实际取值，最终报告中与论文偏差单独成节。

## 3. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 仿真器 | **Isaac Gym Preview 4** | 用户指定；OmniH2O/PHC 等 tracking 代码成熟参考 |
| RL 框架 | rsl-rl（PPO） | 论文用 PPO；与 Isaac Gym 生态配套 |
| 基础代码参考 | **human2humanoid (OmniH2O)**：G1 资产、环境框架、重定向思路 | PHC 为备选；PGMT Stage 1 本质是 HOVER/OmniH2O 式 tracking + 论文新组件 |
| 数据 | **LAFAN1**（Ubisoft LaForge，GitHub 下载，研究用途免费） | 论文指定；Mixamo 骨骼 BVH |
| 本地开发环境 | Windows + conda（torch CPU + numpy + pytest） | 只做逻辑单测与静态检查 |

### ⚠️ 前置风险：Isaac Gym × RTX 5090（Blackwell）兼容性
- Isaac Gym 官方 wheel 绑定旧版 PyTorch（cu11.8 时代），而 Blackwell（sm_120）需要 **torch 2.7+ cu128**
- **M0 必须在服务器上先做兼容性冒烟测试**，决策树：
  1. `isaacgym preview4` + torch 2.7 cu128 → 若可运行 ✅
  2. 社区补丁版 Isaac Gym（Blackwell 适配 fork）→ 若可运行 ✅
  3. 兜底：**转 Isaac Lab**（官方持续维护、原生支持 cu128）。代码按"环境层与策略层解耦"设计，迁移成本可控

## 4. 仓库结构

```
D:\PGMT\
├── 复现方案.md                # 本方案
├── README.md                  # 仓库说明 + 快速开始
├── setup/
│   ├── requirements.txt       # 服务器 Python 依赖（版本锁定）
│   ├── install_server.sh      # 服务器一键安装（含 Isaac Gym 手动下载说明）
│   ├── install_local.bat      # 本机 Windows 开发环境
│   └── smoke_test.py          # 服务器兼容性冒烟测试（M0）
├── pgmt/
│   ├── cfg/
│   │   ├── assumptions.py     # 假设清单 A1-A14 的唯一出处
│   │   ├── ppo_cfg.py         # rsl-rl 配置
│   │   └── env_cfg.py         # 环境/随机化/课程配置
│   ├── envs/
│   │   ├── g1_env.py          # G1 29-DoF Isaac Gym 环境（观测/动作/PD）
│   │   ├── terrain/
│   │   │   ├── generators.py  # 5 族 × L0-L9 地形生成（解析函数）
│   │   │   ├── elevation_map.py  # 21×21 yaw 对齐高程图 + 噪声
│   │   │   └── curriculum.py  # level-based 课程 + 兼容规则
│   │   ├── reference_sampler.py  # 运动段采样 + adaptive sampling
│   │   └── recovery_pool.py  # 摔倒状态池
│   ├── policy/
│   │   ├── history_encoder.py # MHCA(s_hist)
│   │   ├── ifm.py             # MHCA + RoPE（s_int）
│   │   ├── glimpse_encoder.py # 位置选择器 + 双线性采样 + patch MLP
│   │   ├── rope.py            # Rotary Position Embedding
│   │   ├── mhca.py            # 共享 MHCA 层（可选 RoPE）
│   │   ├── mlp.py             # 共享 MLP 构造器（ELU）
│   │   ├── actor.py
│   │   └── multi_head_critic.py  # 3/4 头共享 backbone
│   ├── rewards/
│   │   ├── tracking.py        # upper/lower（root-centric）+ TA 版本
│   │   ├── auxiliary.py       # root/恢复/正则化
│   │   ├── terrain_contact.py # Stage 2 接触组
│   │   └── relaxation.py      # 地形感知松弛
│   ├── train/
│   │   ├── train_stage1.py    # Stage 1 入口
│   │   └── train_stage2.py    # Stage 2 入口（加载 stage1 ckpt）
│   └── utils/                 # 日志、经验回放 buffer、接触标签工具
├── data/
│   ├── download_lafan1.sh
│   └── retarget_lafan1.py     # Mixamo→G1 重定向 + 训练集导出
├── eval/
│   ├── benchmark_dataset.py   # 9600 episodes 匹配数据集（种子固定、可复现）
│   ├── run_eval.py            # 策略评估入口
│   ├── metrics.py             # completion/RMSE/F1/Wilson CI
│   └── ablations/             # NoHeight/CNN/2Glimpse/Fixed/Aggregate-critic 变体配置
├── baselines/
│   └── rgmt_reimpl/           # RGMT 复现（尽力而为，M6）
└── tests/                     # 纯逻辑单测（Windows 可跑）
    ├── test_rope.py
    ├── test_mhca.py           # MHCA 形状/RoPE 位置敏感性
    ├── test_history_encoder.py
    ├── test_ifm.py            # Stage1/2 KV 组成/地形注入
    ├── test_glimpse.py        # 双线性采样数值正确性
    ├── test_actor_critic.py   # 头顺序/Stage1→2 权重重排
    ├── test_policy_integration.py  # 完整策略端到端组装
    ├── test_relaxation.py
    ├── test_terrain.py        # 地形解析函数/高程图采样
    ├── test_reference.py      # 时间偏移采样/自适应采样
    └── test_rewards.py        # 奖励公式（mock 状态）
```

## 5. 里程碑

### M0 环境与骨架（本机 + 服务器冒烟）
- 交付：git init；仓库骨架；`setup/` 全部脚本；`smoke_test.py`（建 128 个 env 跑 10 步 PPO，验证 Isaac Gym×Blackwell）；本机 conda 环境 + `tests/` 可跑通
- 验收：**服务器冒烟测试通过**（决策树第 1/2 步成功，否则切换 Isaac Lab 并更新本方案）；本机 `pytest` 全绿

### M1 数据管线
- 交付：LAFAN1 下载（~144MB BVH，LFS 需 media URL）→ Mixamo→G1 重定向（含脚部接触标签：足速阈值法）→ 训练集/评估集切分（按论文按运动段跨序列采样）；参考帧预计算（含指数偏移采样、修正速度接口）
- 验收：重定向可视化检查（无穿地/关节跳变）；单测覆盖采样逻辑；运动段统计与论文描述一致
- 状态（2026-09-16）：已交付 `data/bvh.py`（解析器）、`data/retarget_lafan1.py`（重定向+批量导出）、`pgmt/envs/reference_sampler.py`（C^K 采样/A13 修正速度/A16 adaptive sampling）、`data/viz_retarget.py`（可视化）、`data/eval_retarget.py`（质量评估，报告在 data/processed/quality_report.csv）；74/74 测试全绿
- 重定向实现要点（数据驱动发现，与预想不同）：
  - LAFAN1 rig 约定：上=+x、前=+y、左=+z（left-handed 变体），旋转序 zyx，cm 单位；绑定姿态不可信（腿折叠）
  - 对齐为**常数** R_align = G1 链基静止朝向 × 循环置换 M_RIG（x→z,y→x,z→y），与运动数据无关；逐链欧拉分解 + 旋转均值居中（去绑定偏移）
  - 根朝向约定：rig → G1 基座（前+x/左+y/上+z）需右乘 Q_MRIG_INV=(0.5,0.5,0.5,0.5)；W 矩阵的 180° 旋转轴角提取有退化特例（对称矩阵），需 (W+I)/2 列提取
  - **评估口径（2026-09-16 校正）**：早期 22cm 的 FK 误差是在错误 root_rot 下测的（qW bug），正确基线 ≈26.5cm（走 22.2/舞 22.9/ground 62.6）；质量报告表与评级已按此口径
  - **实验结论（牛顿细化序贯分解，2026-09-16）**：倾斜链的精确逆（FK 组合互逆）在 FK 位置误差上反而变差（20.9→25.8cm）——精确末端朝向把中间连杆推离源姿态，而跟踪奖励正是连杆位置。已回退到朴素欧拉（文档在 _decompose_chain docstring）
  - **躯干专项（2026-09-16）**：18° 躯干误差的根源是 **FK bug**（torso_link 的关节映射缺失：waist_pitch_joint 定义在 torso_link 元素内，body[:-5] 映射失效 → 腰 pitch 从未应用）。修复后躯干朝向误差 **0.0°**；已加解析回归测试。同时腰链跳过旋转均值居中（绑定脊柱直立、均值=风格前倾 17°，居中会抹掉风格）
  - **万向节保护（2026-09-16）**：`_gimbal_protect()`——3 铰链链中间角近 ±90° 时外角提取退化，奇异区外角用邻域好帧 (sin,cos) 空间插值（80° 硬阈值；更低阈值/锥形过渡会误伤真实手势）。全量速度尖峰 0.31%→0.17%
  - **已知 v1 限制（收尾时点，2026-09-16）**：
    - 躺地/翻滚类（ground 5 序列）：FK 误差 63cm，非直立姿态分解退化——已建议排除出训练集（摔倒池用 fall/push 类 28cm）
    - 中等角度下腕/肘的表示分支翻转（快动作）：残余速度尖峰 ~0.17%，大半为源数据真实快手势（帧间 50-100°），非伪影；完整表示连续性改造列为 P2
    - 足底 −2~−5cm；接触一致率 59-79%
    - 质量报告：`data/eval_retarget.py` → `data/processed/quality_report.csv`（FK 误差总体 26.8cm：走 22.6/舞 23.3/ground 63.3）

### M1.5 IK 精修（2026-09-16 收尾）
- 交付：`data/ik_refine.py`（批量 LM 位置 IK，几何雅可比差分回归锁定三个实现坑：严格祖先 / 子 body 当前朝向轴 / 子 body 原点支点）；M1.5a 腿链（膝/踝 ≤8cm 验收通过）；M1.5b 全链 29 关节位置 IK；M1.5c 接触标签统一协议 + ground 类裁定（A17）
- 最终数字（`python -m data.eval_retarget`）：总体 FK 16.44cm；**非 ground 类均值 ≈14.3cm**；评级 A=46/B=12/C=10/D=9；质量报告同路径，可视化 `eval/viz/*.png`（walk/dance/jumps/ground 各一）
- **≤12cm 原验收线裁定为不可达（几何地板）**：G1 模型**无脊柱**——pelvis→torso_link 仅 4.4cm（腰三关节零长连杆），LAFAN1 Hips→Spine2 ≈31cm。Spine2（25cm）与双肩（22cm）误差是骨架拓扑不匹配的系统分量，任何关节角优化无法消除；踝/膝/手/前臂等可控关键点均 ≤12cm。新验收口径：非 ground 类 ≤15cm + 系统关键点（Spine2/肩）单独报告
- **链接方向残差实验否决（×2）**：两阶段（先位置后方向）与联合单阶段均使肘/手/膝变差（walk1 14.68→15.70cm）——源与 G1 肢体比例不同，方向目标与位置目标几何不一致。`refine_full` 最终 = 纯位置残差（负结果记录在 docstring）
- **接触标签统一协议（M1.5c）**：`contact_labels()` = 足速阈值（A14）+ 高度条件（地面=双踝 z 2% 分位 + 5cm 净空，排除空中缓速帧）+ 窗 5 中值滤波；源侧与 G1 侧共用，一致率在同等协议下比较（现 53-94%，此前循环度量已修复为 vs 源标签）
- **A17 ground 类裁定**：ground1 三序列躺地分解退化（38-116cm），ground2 两序列尚可（16.5cm）但为类内一致性整体排除训练集（摔倒池用 fall/push 类）；`MotionDatabase` 加载时按前缀过滤
- 其余：qpos 导出 wrap 到 (−π,π]（环境 PD 参考按规范角比较）、qvel 由 unwrap 差分；本机 pytest 需 `tests/conftest.py` 设 KMP_DUPLICATE_LIB_OK（Anaconda MKL × torch 双 OpenMP 运行时导致 SVD Abort）；81/81 测试全绿

### M2 Stage 1：Tracking Pretraining
- 交付：g1_env（观测 96+10×96+C^K、PD 控制）、History Encoder、IFM(RoPE)、Actor、三头 critic、Table I 上半部奖励（root-centric）、recovery curriculum、adaptive sampling、PPO 训练入口
- 验收（服务器平地训练）：多种运动（走/跑/舞/蹲/大幅动态动作）可跟踪；平地 episode 完成率 > 90%；body pos RMSE < 0.1m 量级；与 OmniH2O/HOVER 平地水平相当；**保存 stage1 checkpoint（供全部 Stage 2 变体复用）**

### M3 地形系统
- 交付：5 族 × L0–L9 生成器、高程图渲染（含噪声/扰动）、level-based 课程、兼容规则、地形相关随机化与延迟扰动
- 验收：高程图单测（解析高度与采样一致）；可视化各难度地形合理（对齐 A7）

### M4 Stage 2：Perception Injection
- 交付：Glimpse Encoder（位置选择器+双线性采样）、IFM 注入、四头 critic、Table I 下半部接触奖励、tracking relaxation、global position correction、训练入口
- 验收：全基准 completion 显著高于 PGMT-Pretrain（论文 40.02%→87.81%，目标 ≥80%）；L9 ≥ 70%；stairs 上相对消融的优势趋势出现

### M5 基准评估与消融
- 交付：benchmark_dataset（9600 matched episodes，种子固定）、metrics（含 Wilson 95% CI）、4 个感知消融 + Aggregate-critic 变体训练与评估
- 验收：产出对标 Table II / Fig. 3 / Fig. 4 的完整表格与图；数值与论文排序一致（Ours > CNN > 2Glimpse/Fixed > NoHeight，stairs 差距最大）；绝对值偏差在 ±5~10% 内（超出则在报告假设章节解释）

### M6 基线与最终报告
- 交付：RGMT-Reimpl（读 RGMT 论文实现，尽力而为）；SONIC external checkpoint（查询 NVIDIA 是否可获取，拿不到则报告空缺并引用论文数字）；最终复现报告（对标表 + 训练成本实测 + 假设与偏差说明）
- 验收：报告覆盖论文全部实验条目，未完成项有明确说明

## 6. 训练运行矩阵（服务器 4×5090）

| 运行 | 目的 | 依赖 | 优先级 |
|---|---|---|---|
| Stage 1 | 全部后续的基础 | - | P0 |
| PGMT-Ours (Stage 2) | 主结果 | Stage 1 | P0 |
| PGMT-Pretrain 评估 | 主表基线 | Stage 1 | P0 |
| 4 个消融 + Aggregate-critic | Table II/Fig. 3/4 | Stage 1 | P1 |
| RGMT-Reimpl | 基线 | 无 | P2（尽力） |
| SONIC external | 基线 | 外部 ckpt | P2（尽力） |

- 论文实测参考：~8.4 s/iter，29 GiB/卡，15k envs/卡。若单卡显存不足可降 envs 数并在报告记录
- 消融与 Ours 共享 Stage 1 checkpoint，节省计算

## 7. 风险清单

| 风险 | 影响 | 对策 |
|---|---|---|
| Isaac Gym 不支持 Blackwell | 阻塞一切训练 | M0 前置冒烟；兜底 Isaac Lab |
| 论文超参未公开 | 指标偏差 | A1–A14 假设表集中管理 + 报告说明 |
| LAFAN1 获取或许可 | 数据阻塞 | 研究用途免费；备选 AMASS 子集（需重新对齐重定向管线） |
| 重定向质量不达标 | tracking 上限受损 | 参考 OmniH2O/PHC 成熟管线；必要时联系作者 |
| RGMT-Reimpl 工作量 | 基线缺失 | 排 P2；先完成主结果 |
| 本机无法验证训练代码 | 迁移服务器后集中爆错 | 纯逻辑全面单测 + WSL2 小规模冒烟（可选） |
| 训练时长 | 多 run 排队 | 优先级排序；checkpoint 复用；先跑 Ours+Pretrain 再跑消融 |

## 8. 不做的事（范围外）
- 真机 G1 部署、LiDAR 建图、Jetson 推理
- teleoperation（PICO/GMR）与 motion matching 命令源
- 真机扰动/摔倒恢复实验
