# 下一批执行计划：站立学习接口与参考病因定位

执行状态：本页登记的 CPU 接入/病因定位及两条件各 20 更新已完成，合计 15360 transitions，
GPU 已释放。40 次更新的接口复核通过；随机动作子步峰值仍达 45.17/54.80 rad/s。
参考独立 v2 修正了已证实的 IK 迭代不足，但悬空和腰贴限未解决，仍隔离。
见[实际执行报告](standing_learning_20260922.md)。100 更新和匹配评估尚未启动。

下文保留执行前的设计和预算，“当前/下一次/应”描述的是预登记时状态。
执行前原文另存 `runs/standing_learning_20260922/source_before/docs/standing_learning_plan_20260922.md`。
本页原为 2026-09-22 资产/载荷对照完成后的规划。
依据：[实际结果](asset_standing_validation_20260922.md)。候选固定 PD 平均首次存活
1.47→2.29 s、共同窗口倾角 20.39°→3.28°，但仍 0/4 完成 10 s。
该结果支持检验反馈学习，不支持宣布动态平衡已解决或直接扩大 Stage 1。

## 1. 先完成 CPU 准备

### 训练入口加载冻结候选

当前 `pgmt/train/fixed_clip_diagnostic.py` 仅支持 zero/clip_start Actor 初始化，
尚不能直接使用 `fixture_v4/candidate.json` 和 `starts.pt`。
应最小扩展该诊断入口，读取并验证候选、起点、资产、参考和代码指纹。
配对启动器在同一 CPU 准备阶段生成两组初始证据、冻结配置及停止预算。

- 使用已验证的静止参考；实际/参考姿态仍 q=0，沿用 nominal reset。
- 使用原来的直立、roll ±1°、pitch +1°四个起点，各重复四份构成 16 环境。
  副本不算独立种子。两条件采用完全相同的起点和采样规则。
- 对照组目标中心为零，候选组为冻结的载荷目标；目标中心只初始化 Actor 偏置，
  训练时由策略输出动作，不强制覆盖动作，也不改为残差控制。
- 从匹配的新策略开始，除 Actor 目标偏置外初始参数一致；探索标准差、优化器、
  critic completion、学习率日程和随机种子匹配。
- PD 80/2、奖励项/权重、A23、终止规则、动作分布和网络结构维持当前配置。
  静态诊断继续关闭 recovery、自适应采样、动力学随机化、观测扰动和执行延迟；
  这些简化明确只用于诊断，不替代正式论文流程。

拟改动位置主要为 `pgmt/train/fixed_clip_diagnostic.py`、配对启动/分析脚本及相关回归测试。
不因本轮尚未站稳而修改 PPO 算法或奖励权重。新入口 CPU 检查通过后才使用 GPU。

### 原八片段病因定位

读取已有完整源序列与新旧产物，先回答三个具体问题，形成逐片段证据表：

1. 哪个时间点、哪个碰撞体决定了全序列最低点？当前抬高量来自真实支撑姿态、
   源地面偏差还是重定向后穿地？保留源帧索引和变换记录。
2. 慢走腰 pitch 贴限主要发生在源映射、解析 retarget 还是 IK 精修？
   对比身体点误差及每阶段关节值，区分形态不可达与求解器问题。
3. low_motion_2 的 0.2922 rad 相邻跳变出现在哪一关节/源帧，
   是否由源动作、分解分支、限位投影或 IK 优化产生？

这批 CPU 工作先定位原因，不承诺换一个高度常数就能修好。
若证据支持新修正，再生成独立 v2，使用同样八片段检查；原数据和 v1 均保留。
支撑期脚间隙须与既有 2 cm 查询容差相容，摆动期应保留抬脚；关节合法、根与关节连续、
身体点误差和贴限状况均需报告。不得通过逐帧硬贴地或事后换片段制造通过结果。
这八片段暂不进入下述站立训练。

## 2. 下一次 GPU 工作限于两组各 20 更新

| 条件 | 环境 | rollout 步/更新 | 更新数 | transitions |
|---|---:|---:|---:|---:|
| 原目标初始化 | 16 | 24 | 20 | 7680 |
| 载荷候选初始化 | 16 | 24 | 20 | 7680 |
| 合计 | | | | **15360** |

两组串行，首轮使用同一个固定种子。学习率仍按 1000 更新日程计算，20 仅为停止点。
第一批真实随机动作 rollout 计入上述预算，检查初始化在探索动作下是否仍数值稳定。
保留完整有界 rollout、动作/latent/log-prob、价值、终止和 reset 前终态、更新前后参数。
本批补存完整 5 ms 子步速度及控制步 EE 原始速度，避免只有速度峰值而无法复查事件。

验收关注：执行动作及 log-prob 一致；GAE/终止语义正确；物理与参考时间对齐；
奖励分项可复算；KL 控制和有效 Actor 更新正常；数值有限、目标合法。
明确错误发生时保存证据并停止对应运行，不修改门槛后自动续跑。
存活、倾角和跟踪误差作为训练内趋势报告；20 更新无明显改善不等于不能学会。
没有真实 timeout 则标记该分支未覆盖，不把 CPU 单测代替物理验证。

达到停止点即保存 checkpoint、退出进程、核对 GPU 释放；分析随后在 CPU 完成。
本批不自动串接冻结策略评估、100 更新或完整 Stage 1。
因此本批可以判断学习接口是否正确，不能单凭训练内指标宣称学习收益。

## 3. 接口通过后才决定多种子学习

后续保留原先登记预算，从匹配的初始策略重新开始，20 更新诊断结果不当作训练起点：

- 两条件 × 3 种子 × 16 环境 × 24 步 × 100 更新：**230400 transitions**。
- 匹配 initial/final、确定性/随机策略，每次 4 起点、最多 10 秒：
  2 × 3 × 2 × 2 × 4 × 500 = **最多 48000 transitions**，单列评估预算。
- 训练完成即释放 GPU，评估可以后置；完成评估前不宣布学习改善。

比较配对存活/完成率、倾角、关节和身体误差、共同存活窗口、力矩及速度尾部。
正式扩展动作前，需要多种子一致的行为改善和预定微扰下可重复的 10 秒完成，
不能只证明跌倒时间延后。未达标则依据具体失败证据定位，不自动延长到 200/500 更新。

## 4. 恢复正式流程的条件

站立学习出现可重复收益、参考质量检查通过后，再接入少量小幅动作，随后扩到完整动作库，
并逐项恢复与新数据绑定的 recovery pool、自适应采样和随机化。
Stage 2 仍需验证过的 Stage 1 prior 以及新参考地面与真实地形坐标的衔接。
旧 checkpoint 不绕过导数/放置兼容检查；旧 v4 的最终评估继续后置。

载荷候选、源平面和离散差分属于独立复现的工程约定，应记录来源和检验范围；
不把它们称为论文作者明确公开的实现。当前计划不改变位置目标 + PD、共享 IFM、
多头 critic、奖励项/权重、恢复课程、自适应采样及两阶段训练的正式流程。

**上述首批已完成；后续优先定位剩余子步速度事件和参考支撑/形态目标问题。**
多种子 100 更新及正式扩规模仍须依据新证据另行决定，不因接口通过自动启动。
